# Estado del subproyecto — «Alan & Aura Académico»
**Propósito:** decir en qué fase está el subproyecto y qué sigue. Es el primer archivo a leer al rehidratar contexto. **Fecha de creación:** 2026-07-12 · **Última actualización:** 2026-08-05 · **Versión:** v3.7 (**`SD-48`: el expediente del `CDR` queda completo**. El acta pasa a `v2.0` con su resumen ejecutivo y la trazabilidad hallazgo→decisión; §Qué sigue apunta ya a **`ARQ-01`**; el veredicto de la línea 13 deja de ser el propuesto; `AGENTS.md` y `CLAUDE.md` dejan de declarar la fase 2 «completa salvo la compuerta»). v3.6 (**`SD-47`: la QUINTA verificación, acotada, SOSTIENE el veredicto** — cero Críticos y cero Mayores— y con ella el límite de `IEEE 1028 §6.5.6.5` declarado para `SD-45`/`SD-46` **queda cubierto**. Se cierran sus dos hallazgos: `VRI-01`, notas que no convergieron, y `VRI-02`, un falso negativo nuevo del comprobador). v3.5 (**`SD-46`: el `CDR-01` queda CERRADO**. Se resuelven los dos últimos Moderados —`CVI-04` decodificando y regenerando el `plantuml-src` sin PlantUML, y `ECU-06 FE-07` por su skill dueña— y el líder determina el veredicto: **`Aceptado con verificación de retrabajo`**, con cero Críticos y cero Mayores. **Siguiente hito: `ARQ-01`**). v3.4 (**revisión interna final antes del veredicto**, dos subagentes: la nota de recuento seguía en 30 filas con la tabla en 34, y en los `DS` se corrigen `RI-1` y `RI-2` — el mismo tipo de defecto que `CVI-03`, sobreviviendo dentro de su corrección). v3.3 (**SD-45: `R3` estaba afirmada, no implementada**. La cuarta verificación confirmó que `SD-44` es un replanteo real pero refutó `R3` — un `loop` tiene un inicio concreto y una nota no cambia su semántica. Los cuatro bucles se acotan al paso que manda su `ECU`, la cancelación sale del bucle, el comprobador se reescribe con 17 fixtures y **ninguna fila abierta bloquea ya el paso al código**). v3.2 (**cabos antes de presentar el paquete a la cuarta verificación**: §Qué sigue anunciaba como próximo hito una re-verificación **que ya ocurrió dos veces** y daba `PER-H2` por el pendiente que más pesa **estando cerrado ocho puntos más abajo, en este mismo archivo**; la frase del recuento señalaba la fila **26**, cerrada, en vez de la **30**; el preámbulo seguía diciendo «los doce» con 30 filas; y un párrafo **duplicado** contaba tres deudas de `ARQ-01` frente a cuatro, con una cláusula sobre `RF-24` ya falsa. Se corrige **antes** de entregar, no se deja para que lo encuentre el revisor. `H-24` comprobado contra el regex: **cerrado por `SD-42`**, y el tablero ya lo decía —es el acta la que está desfasada, y eso lo arregla el revisor). v3.1 (**SD-44, la convención de desenlaces**: la **tercera** verificación independiente refutó **8 de 13** ramas de `SD-43` y activó el **freno de Wiegers**. En vez de una cuarta pasada de parcheo se fija **cómo** se modelan desenlaces —R1 a R4—, se instala como comprobador y se prueba haciéndolo fallar. Cierran `TVI-01`, `TVI-02`, `TVI-04`, `TVI-05` y `TVI-06`; abre la **cuarta verificación**). v3.0 (**SD-43, el tercer intento sobre `VI-01` y el primero correcto**: la segunda verificación independiente **refutó** el cierre de `SD-41` —las diecisiete `ECU` dicen «Vuelve», ninguna «Termina»— y las 13 ramas pasan a **`loop` + `alt`**, con los 22 `break` legítimos intactos. Se cierran `SVI-02` y `SVI-03`, se deshace la propagación falsa en **seis pasajes** y entra **`SD-43-H1`**: los `break` dentro del `loop` de `DS-06`, que **nadie había visto**). v2.9 (**SD-42, el instrumental**: se cierran `VI-07`, `VI-08`, `SD-40-H1` y `H-24`, los cuatro **probados haciéndolos fallar**; el verificador gana el **bloque 6**, que caza lo que el 3 no puede ver por estar ya comiteado. Ese bloque destapó **dos artefactos más** con el defecto `H-25` —van ocho— y **ocho historiales desordenados**, todos reparados. Se cierran las filas 16, 18 y 22). v2.8 (**SD-41 fase 3, `VI-03` cerrado**: `ECU-08` se contradecía consigo mismo tras `H-17` — y eran **cinco** sobrepromesas, no las tres del acta. Entra la fila **23**: la misma sobrepromesa sigue viva en `VIS-01 OBJ-6`, que además se contradice con su propio §Alcance). v2.7 (**SD-41, los dos Mayores del CDR cerrados**: `VI-01` corregido rama por rama —~~trece de los diecisiete `opt` resultaron `break` legítimos~~ **refutado en `SD-43`: los diecisiete dicen «Vuelve»**— y `VI-02` con la costura `DR→DOP→COD→CP` repetida por skill dueña, en sustitución 1:1. Se cierran las filas 19 y 20; quedan **10 abiertas**. Dos hallazgos que el acta no vio: el invariante ATÓMICO de `DR-04` y el parámetro perdido de `revocarCapaBase()`. **Falta re-verificar: quien aplicó no puede firmar.**). v2.6 (**SD-40, la verificación independiente**: la ejecutó **otro modelo** —Codex `gpt-5.6-sol`— sobre el retrabajo de Claude Opus y emitió `CDR-01 v1.4`. **Refutó `H-01` y `H-02`**: los 17 `opt` caen al flujo de éxito, así que cancelar un borrado sigue borrando. Se **cierra** el pendiente 15 y **entran cuatro**: 19 `VI-01`, 20 `VI-02`, 21 `VI-03` y 22 los dos huecos del verificador. Se corrige el recuento del tablero —decía «once abiertos» y llevaba dos versiones sin cuadrar— y se propaga el canon a **once** puntos vivos, cuatro que el acta no vio. **El veredicto sigue en `Reinspección requerida`, ahora por hallazgos propios.**). v2.5 (**SD-39, el retrabajo del `CDR-01`**: los 20 hallazgos resueltos y 8 nuevos declarados; se **cierran** los pendientes 12, 13 y 14 y **entran cuatro nuevos** —15 la verificación independiente, 16 el punto ciego del bloque 4, 17 los defectos del generador de la skill, 18 la nota de versión de `MD-01`/`DCU-01`—; seis hechos canónicos movidos y `H-29` creado. **El veredicto sigue en `Reinspección requerida` a propósito.**). v2.4 (`CDR-01 v1.1`: cobertura completa — 14/14 especificaciones y 168/168 obligaciones emparejadas, los 8 validadores sobre los 31 `.puml`, tres comprobaciones cruzadas nuevas. **20 hallazgos y 3 mayores**, no 13 y 2). v2.3 (SD-37: **el CDR se ejecutó** — `CDR-01`, veredicto `Reinspección requerida`; entran las filas 13 y 14 al tablero de pendientes y se reencuadra la 12). v2.2 (SD-36: propagación que `SD-35` dejó a medias — el canon y tres artefactos seguían declarando `PER-H2` abierto y `RF-24` incumplido). v2.1 (SD-35: `PER-H2` cerrado por `ADR-004` — **`RF-24` pasa a cumplirse**). v2.0 (SD-33: `PER-H5` **cerrado** por `ADR-003` — el MVP no respalda el almacén de datos personales; `RF-24` pasa de dos excepciones abiertas a una). v1.9 (SD-32: el diagrama de clases de diseño está hecho; el siguiente hito es el **CDR**; `COD-01` cerrado y entra un pendiente nuevo, el render del `.svg` de `MC-01`). v1.8 (SD-31, segunda pasada: canon y punteros al día — el «siguiente artefacto» deja de ser la secuencia, los validadores pasan a cinco y las versiones citadas se alinean con sus fichas). v1.7: nueva sección **«Pendientes declarados»**, que reúne los once que estaban dispersos —`PER-H5` y `COD-01` entre ellos—; y los 14 SVG de robustez regenerados sin colisiones). v1.6: SD-30 completo, con los 14 diagramas de secuencia, sus 181 casos de prueba y los 7 hallazgos aplicados; robustez en **262 elementos**. v1.4 corrigió el conteo de `.puml` de la Fase 2: son **16**, no 17.

---

## Dónde estamos
Fase **1 cerrada** y **Fase 2 (ICONIX) avanzada**: producidos y verificados los cinco primeros artefactos — el **modelo de dominio `MD-01 v1.6`** (`docs/06_dominio/`, 16 clases), el **diagrama de casos de uso `DCU-01 v2.2`** (`docs/07_casos_uso/`, 14 casos de uso), la **especificación textual `ECU-00…ECU-14`** (`docs/07_casos_uso/especificaciones/`), el **análisis de robustez `DR-00…DR-14`** (`docs/07_casos_uso/robustez/`, 262 elementos) y la **compuerta ICONIX `RPD-01`** entre análisis y diseño detallado (`docs/07_casos_uso/RPD-01_revision_preliminar_diseno.md`).

Ese conjunto pasó por una **primera pasada completa de correcciones** — `PDR-01` (`docs/00_gobernanza/PDR-01_primera_pasada_correcciones.md`), motivada por la retroalimentación docente de 4 puntos (`RET-01`) y los 15 hallazgos de desambiguación del certificado de robustez v1 — y luego por la **Revisión Preliminar del Diseño** propiamente dicha (`RPD-01`, skill `iconix-pdr-review`), cuyo veredicto es **Aceptado con verificación de retrabajo** (un hallazgo mayor real, corregido en la misma sesión; el resto, moderado o menor, ya cerrado o declarado). **No confundir los dos "PDR":** `PDR-01` es el registro de gobernanza de la pasada de correcciones; `RPD-01` es el acta formal de la compuerta ICONIX (*Preliminary Design Review*) — el acrónimo coincide por casualidad y cada documento lo aclara en su cabecera.

**Estado medido y reproducible:** los **cinco validadores en 0 errores** (`MD-01`, `DCU-01`, las 14 especificaciones —también 0 advertencias—, los 14 diagramas de robustez con `--domain MD-01` y los **14 de secuencia**, estos últimos con 6 advertencias, todas la excepción declarada `E-2`). Las especificaciones cubren el **100 % de sus 76 flujos** con un criterio de aceptación asociado. Los **26 RF** tienen caso de uso propio y único, y las **16 clases** del dominio se manifiestan en al menos un caso de uso y en al menos un diagrama de robustez (`TRZ-01` §5.1 y §5.2, verificado por script contra los `.puml`, 0 discrepancias). No hay código ni pilotos, por diseño. La rama de correcciones (`pdr-01-correcciones`) ya está fusionada y publicada en `main`. **El diagrama de clases de diseño está hecho** (`SD-32`, `MC-01`, retrabajado en `SD-39`): **43 clases** —16 del problema con nombre idéntico a `MD-01` y **27 del espacio de la solución**—, **201 operaciones**, **51 atributos**, 11 enumerados y **80 relaciones**, con el validador de la skill en 0 errores y la verificación exacta de procedencia sin discrepancias, y con su `.svg` **generado y mirado**.

**Y la compuerta final ya se cruzó** (`SD-37`, `CDR-01`; **retrabajada en `SD-39`**): la Revisión Crítica del Diseño corrió en modo completo. En su día quedaron **nueve de las diez reglas ejecutadas** —la #2, generar e inspeccionar las cabeceras de código, diferida con disparador—; **`SD-39` ejerció también la #2**, así que están las diez. La v1.0 produjo **13 hallazgos con dos Mayores**; la v1.1, **20 con tres**; y el retrabajo destapó **8 más**. Su veredicto, **determinado por el líder el 2026-08-05** tras **cinco verificaciones independientes**, es **`Aceptado con verificación de retrabajo`** —cero Críticos y cero Mayores— y ~~`Reinspección requerida`~~ era el propuesto mientras el retrabajo seguía abierto. **La compuerta está cerrada.** Las once verificaciones limpias del acta y una tasa de **0,37 hallazgos por página** contra la base medida de 0,4 describen un diseño maduro; lo que la escala de IEEE 1028 refleja es que dos defectos afectan al **comportamiento** y no a la forma. La reinspección que sigue es **acotada** —solo el retrabajo—, no una repetición de la revisión.

## Fases del subproyecto

| Fase | Descripción | Estado |
|---|---|---|
| 0 | Delimitación de alcance (plan generado con Codex) | ✅ cerrada (insumo externo) |
| 1 | **Auditoría del plan + artefactos documentales mínimos** (esta fase) | ✅ **cerrada (2026-07-12)** |
| 2 | Análisis y diseño ICONIX (dominio → CU → especificación → robustez → **RPD** → secuencia → clases → **CDR**) | 🟨 **compuerta ejecutada, retrabajo abierto** — `MD-01` ✅ · `DCU-01` ✅ · `ECU-00…14` ✅ · `DR-00…14` ✅ · `RPD-01` ✅ · `DS-00…14` + `DOP-01` + `CP-00…14` ✅ (SD-30) · `MC-00`/`MC-01`/`COD-01` ✅ (SD-32) · **`CDR-01` ✅ (SD-37): veredicto `Reinspección requerida`, 13 hallazgos con 2 mayores y corrección acordada** |
| 3 | Construcción del MVP (React + Vercel · Lambda/API Gateway · DynamoDB/S3 · LLM gobernado · gate de seguridad) | ⬜ planificada — stack decidido en `ADR-002` |
| 4 | Verificación, despliegue académico y entrega | ⬜ planificada |

## Primera pasada de correcciones (`PDR-01`) y compuerta `RPD-01` — resumen (2026-07-31)

Detalle completo en `PDR-01_primera_pasada_correcciones.md` y `RPD-01_revision_preliminar_diseno.md` (`docs/07_casos_uso/`); evidencia punto por punto de la retroalimentación docente en `RET-01_retroalimentacion_docente.md`.

| Artefacto | Antes | Después |
|---|---|---|
| `MD-01` | v1.2, 12 clases, 12 relaciones | **v1.4**, 16 clases, 17 relaciones |
| `DCU-01` | v1.0, 4 actores, 10 casos de uso | **v2.1**, 5 actores, 14 casos de uso |
| Especificaciones | 10 + índice, 18 errores de validador | **14 + índice, 0 errores y 0 advertencias** |
| Robustez | 10 diagramas, 179 elementos | **14 diagramas, 262 elementos**, 0 errores |
| Compuerta ICONIX | no ejecutada | **`RPD-01`: aceptado con verificación de retrabajo** |

Decisiones nuevas que introdujo: el `Consentimiento` se separa en **capa base** y **capa de personalización**; el límite por mensaje sube de 1.500 a **2.500 caracteres**; «acompañante» se declara alias de producto en uso activo de `Personaje`. Reversiones declaradas (no disimuladas): `Visitante` pasa a ser clase de dominio; `EventoOperativo` deja de ser «vista derivada, no clase».

## Entregables de la Fase 1 (esta ejecución)

| ID | Artefacto | Estado |
|---|---|---|
| — | Gobernanza (README, AGENTS, CLAUDE, índice, cápsula, estado, decisiones, changelog, manifiesto) | ✅ |
| — | `00_AUDITORIA_PLAN_CODEX` — auditoría breve del plan | ✅ |
| VIS-01 | Visión, objetivos, alcance, exclusiones, definición de «MVP terminado» | ✅ |
| ADR-001 | Decisiones técnicas (stack) con condiciones de reversa | ✅ — **D1/D2/D5 superadas por `ADR-002`** |
| ADR-002 | Reversión del stack a arquitectura sin servidor (React + Vercel + AWS) — *añadido 2026-08-01, SD-29* | ✅ |
| — | `HECHOS_CANONICOS.md` — fuente única de las cifras repetidas + `scripts/verificar_coherencia.py` — *añadidos 2026-08-01, SD-29* | ✅ |
| MV-01 | Modelo verbal **consolidado** (vistas onboarding/conversación/seguridad/administración) + contrato E4 simplificado; apto para extracción de dominio | ✅ |
| REQ-01 | RF/RNF + requisitos de calidad (25010:2023 + GQM + umbral) + reglas tipadas | ✅ |
| PRIV-01 | Privacidad, minimización, retención, no-persistencia | ✅ |
| PER-01 | Mapa de persistencia (7 entidades, reglas transversales, hallazgos) — *añadido 2026-07-25, SD-25* | ✅ |
| SEG-01 | Protocolo de seguridad (gate binario + fallback) | ✅ |
| NORM-01 | Puente normativo (mini D6-bis) | ✅ |
| TRZ-01 | Matriz de trazabilidad (cero huérfanos) | ✅ |
| PLAN-01 | Plan de proyecto de 1 mes | ✅ |

## Fuente primaria archivada y reconciliada (SD-16/SD-17, 2026-07-12)
`00_PLAN_CODEX_ORIGINAL.md` — texto completo verbatim del plan de Codex. **Reconciliación APLICADA** (SD-17) contra los 5 puntos identificados al comparar con la reconstrucción forense previa:
1. ✅ Historial de sesión (hasta 4 intercambios, no "cero historial") — MV-01 RN-02.2/RN-03, REQ-01 RF-09/RNF-04, PRIV-01 PRIV-R1, CONTRATO C-4/CA-4.
2. ✅ Límites de tasa exactos (3/min, 30/día, 20s timeout, **2.500 caracteres**, 350 tokens) — MV-01 RN-02.8/RN-02.9, REQ-01 RF-25/26/RNF-10, TRZ-01. El límite por mensaje se elevó de 1.500 a 2.500 en el PDR-01.
3. ✅ Inventario de PRIV-01 mapeado 1:1 a las 7 entidades del plan (`User`/`ConsentRecord`/`InitialConversationProfile`/`PlatformSetting`/`DailyUsageCounter`/`OperationalEvent`/`AdministrativeAction`).
4. ✅ Tabla de personajes del contrato enriquecida (P-1..P-8: función/tono/respuesta típica/preguntas/sugerencias/longitud/límites).
5. ✅ Plantilla del fallback citada en SEG-01 §5 (sin números fijos, respeta SD-12).
6. ⬜ Tabla de endpoints (§4.9 del plan) — diferida a `ARQ-01`, junto con el diseño de claves de DynamoDB y el inventario físico de S3. `ARQ-01` llega **después del diagrama de clases de diseño y de su CDR** (`ADR-002 §1`): fijar esquemas mientras el diagrama de clases aún puede mover atributos y operaciones garantiza retrabajo.
7. ✅ **Cápsula de 5 campos (SD-22, RA-01 cerrada, 2026-07-16):** el LLM recibe `ContextoInicialConversacionalV1` (5 de contenido + 2 metadatos), no la cápsula de 3 campos — MV-01 RN-01.3/§3/§13.1, REQ-01 RF-04/05, PRIV-01 PRIV-R1/§2, MD-01 §6, ECU-04/05/06/00. Sigue siendo minimización; PRIV-R9 intacta.

## Corrección de alcance (SD-15, 2026-07-12)
Verificación forense contra el plan de Codex: se restauraron las 3 funciones reales del administrador (directorio, métricas agregadas, kill switch) y las de cuenta del usuario (registro/login, reinicio de perfil, revocación, eliminación en cascada, manejo de errores), y se quitó la edición administrativa que el plan excluye (recursos/textos **por entorno**). Propagado a VIS-01, MV-01 (v2.1, +`DisponibilidadDelChatbot`), REQ-01 (v1.1, +RF-19…26), PRIV-01, CONTRATO, MD-01 (12 clases) y TRZ-01. Detalle en `CHANGELOG.md` (v0.4.0) y `REGISTRO_DECISIONES.md` (SD-15).

## Verificación de cierre (Fase 1)
| Criterio | Resultado |
|---|---|
| Checklist de 11 rasgos de E8 en el MV | ✅ 11/11 en MV-01 (consolidado; vistas Onboarding·Conversación·Seguridad·Administración) |
| Cero requisitos huérfanos (TRZ-01) | ✅ verificado (TRZ-01 §5) |
| GQM + umbral por requisito de calidad | ✅ RC-01…RC-10, todos con umbral (REQ-01 §3) |
| Cláusulas ISO reutilizadas de D6-bis, sin invención | ✅ NORM-01 (5 filas *safety* + §3.4 a nivel [V-cláusula]) |
| Aislamiento (`git status` solo la carpeta nueva) | ✅ solo `subproyecto_academico_alan_aura/` untracked |

### Matriz de canon §5 por artefacto
Leyenda: ✅ afirmado/cumplido · ➖ no aplica al artefacto.

| Artefacto | No sobre-claim | Minimización | No punitivo | No persistencia chat | Disclosure/adultos |
|---|---|---|---|---|---|
| VIS-01 | ✅ | ✅ | ✅ | ✅ | ✅ |
| ADR-001 | ➖ | ✅ (D2) | ➖ | ✅ (D2) | ✅ (D7) |
| MV-01 (consolidado) | ✅ | ✅ | ✅ | ✅ | ✅ |
| Contrato | ✅ (C-2) | ✅ (C-4) | ✅ (C-6) | ✅ (C-5) | ✅ (C-1) |
| REQ-01 | ✅ | ✅ (RC-04) | ✅ (RN-08) | ✅ (RNF-03) | ✅ (RF-01) |
| PRIV-01 | ✅ | ✅ | ✅ (PRIV-R6) | ✅ (PRIV-R2) | ✅ (PRIV-R8) |
| SEG-01 | ✅ (§2) | ✅ | ✅ | ✅ | ✅ |
| NORM-01 / TRZ-01 | ➖ (trazan) | ➖ | ➖ | ➖ | ➖ |
| PLAN-01 | ✅ (DoD) | ✅ (DoD) | ✅ | ✅ | ✅ |

*(Ver `CHANGELOG.md` para el registro del hito.)*

## Arquitectura técnica decidida (`ADR-002`, 2026-08-01)

El stack pasa de **Django 5.2 + SQLite + PythonAnywhere** a **React 19 + Vite + TypeScript sobre Vercel**, con backend **sin servidor** en AWS (**Lambda** en Node 22 tras **API Gateway**, **DynamoDB** como base operativa y **S3 versionado** para configuración, activos y respaldos), componentes **Tailwind v4 + shadcn/ui sobre Base UI**, y **autenticación propia** (Argon2id + cookie firmada). **Groq `gpt-oss-20b` se mantiene.**

Tres cosas que conviene tener presentes al leer esto:

1. **Ninguna condición de reversa declarada disparó el cambio** — fue decisión del equipo (SD-29), no contingencia prevista. `ADR-002 §0` lo dice expresamente.
2. **La fase 2 no se tocó.** Los 16 `.puml` y las 14 `ECU` no contenían ni una mención del stack; solo se neutralizaron **dos líneas de anexo** en `MD-01.md` y `DCU-01.md`, y por categoría, no por producto, para que no vuelvan a caducar. Es el rendimiento de haber mantenido el dominio libre de tecnología.
3. **Se decidieron motores, no esquemas.** El diseño de claves, la tabla de *endpoints* y el inventario de S3 van a `ARQ-01`, **tras el CDR**.

**Consecuencias nuevas declaradas y no resueltas:** la protección CSRF hay que construirla (frontend y backend dejan de compartir origen), el arranque en frío presiona sobre `RC-05`, la región de AWS abre una pregunta de residencia de datos bajo la Ley 1581 (`PRIV-01 §4.1`), y ~~los respaldos en S3 escapan al borrado en cascada~~ — **`PER-H5`, cerrado en SD-33** por `ADR-003`: el almacén operativo **no se respalda**, así que esa consecuencia dejó de existir en vez de resolverse. Las otras tres siguen abiertas.

## Qué sigue
1. **`ARQ-01`, el diseño físico — y no queda nada del `CDR` por delante.** La compuerta **está CERRADA** (`SD-46`, veredicto del líder: `Aceptado con verificación de retrabajo`), y **ninguna fila abierta del tablero bloquea el paso al código**.

   **`ARQ-01` decide** lo que `ADR-002 §1` le difirió: claves de DynamoDB —particiones, ordenación, índices secundarios, TTL, tabla única frente a múltiple—, la tabla formal de *endpoints* y contratos de API, el inventario físico de S3, e IAM, red, infraestructura como código y *runbook*. **Hereda cuatro deudas:** CSRF, el *endpoint* de cierre de sesión que reabre `RA-01` de `ECU-03`, `PER-H4` y `H-09`.

   **La condición que lo bloqueaba ya se cumplió.** `ADR-002` lo decía sin rodeos: *«el diagrama de clases aún puede mover atributos y operaciones; fijar claves antes de esa convergencia produce retrabajo garantizado»*. Con el `CDR` cerrado, `MC-01` queda congelado en **43 clases / 201 operaciones / 51 atributos / 80 relaciones**.

   **Y algo que conviene decidir al empezarlo:** es el primer artefacto que toca **decisiones de seguridad reales** —IAM, CSRF, política de cookies—, así que probablemente merezca **su propia compuerta de revisión**, no la del `CDR`, que ya cerró.

   **Cómo se llegó aquí, en una línea por vuelta.** `H-01` puso `break` donde el texto pedía volver; `SD-39` lo cambió a `opt`, que **cae al flujo de éxito**; `SD-41` restauró `break` con una lectura de las `ECU` que era **falsa**; `SD-43` puso `loop` + `alt` y **seguía mal**, porque el operando de éxito quedaba **vacío** y la continuación **fuera** del `alt`. **Tres verificaciones independientes, y las tres refutaron.**

   **`SD-44` no es un quinto parche: es el replanteo que el freno de Wiegers exigía.** Fija la **convención de desenlaces** —R1 el sufijo de éxito vive **dentro** de su operando · R2 un `break` en un `loop` solo vale si lo que sigue es **limpieza común** · R3 el punto exacto de reentrada es normativo en la `ECU` y verificable en el `CP`, y el `DS` **no lo dibuja** · R4 la guarda nombra todas las salidas— y la instala como **comprobador versionado**, `docs/07_casos_uso/secuencia/scripts/barrido_desenlaces.py`, **probado haciéndolo fallar**.

   **Lo que toca verificar NO es la misma revisión otra vez** —el freno lo prohíbe expresamente— sino **material nuevo**: ¿la convención **resuelve** la clase de defecto o solo la **desplaza**?, y ¿el comprobador **caza lo que dice cazar**, sobre todo en sus falsos negativos? La ruta está probada tres veces: **Codex GPT-5**, encuadre adversarial, `codex exec --sandbox danger-full-access`, que es lo único que le da Python en Windows. **Quien aplicó no firma** (`IEEE 1028 §6.5.6.5`).

   **Y la regla de parada ya está decidida, para no entrar en una quinta vuelta:** si la cuarta trae Mayores de la **misma clase** —retornos y terminaciones—, **no se repite**: el líder determina `Aceptado con verificación de retrabajo` y se declara por escrito que el **modelado exacto de retornos queda fuera del alcance del diagrama de secuencia**, con su cita de fuente, trasladado a los `CP` y a la fase 3 con responsable y fecha. El límite **nunca** convierte un Mayor abierto en aceptación: aquí no se convierte, se **reubica y se declara**.

   > **⚠ Lo que `SD-41` creyó encontrar, y era FALSO.** ~~`VI-01` no eran diecisiete errores: **trece de los diecisiete `opt` resultaron ser `break` legítimos**, porque su `ECU` dice «Termina» y no «Vuelve». Solo cuatro estaban mal.~~ **Refutado en `SD-43` por `SVI-01`** (Mayor, de la segunda verificación independiente): las **diecisiete** filas de `ECU` dicen «Vuelve al paso N» o «Cancela y vuelve al paso 1»; **ninguna dice «Termina»**. La relectura que `SD-41` presume haber hecho **no se hizo bien**, y por tercera vez el operador se eligió sin leer el desenlace. Las trece pasan a `loop` + `alt` en `SD-43`. La causa de fondo se repitió dos veces —en `H-01` y en su corrección— y es la misma: **elegir el operador por categoría (`FE`→`break`, `FA`→`opt`) en vez de por el desenlace que el texto declara**. Se corrigió releyendo cada fila, no aplicando una regla.

   > **Y dos cosas que el acta no había visto**, ambas destapadas al propagar: la nota de `DR-04` seguía declarando el borrado en cascada **ATÓMICO** —la promesa exacta que `H-02` derribó, una capa más adentro que los controladores que `VI-02` señalaba—, y `COD-01` proyectaba `revocarCapaBase()` **sin el parámetro `fecha`** que `H-11` le añadió y `SD-39` aplicó solo en `MC-01`.

   > **El punto en que salió mejor: la hizo otro modelo.** El plan de `SD-39` suponía una segunda sesión del **mismo** modelo, que solo mitiga el sesgo de recuerdo. La ejecutó **OpenAI Codex `gpt-5.6-sol`** sobre el retrabajo de **Claude Opus**, con encuadre adversarial —refutar, no confirmar—. Un modelo distinto **no comparte los puntos ciegos** del que aplicó, y eso se notó: encontró lo que ninguna sesión del autor iba a encontrar sobre sí misma. **La limitación se declara igual y sin adorno:** sigue **sin satisfacer `IEEE 1028 §6.5.6.5`**; la independencia real la da el equipo humano de `CLAUDE.md §2`, y **el veredicto lo determina el líder**.

   > **El punto en que salió peor: `H-01` y `H-02` no pasaron.** La corrección de `H-01` sustituyó 17 `break` por `opt` —el recuento es exacto, 33 correctos y 17 sustituidos— pero **`opt` no vuelve al paso que el texto pide: solo salta su cuerpo y sigue**. Así, en `DS-04` el usuario cancela la eliminación y el flujo **continúa a la cascada de borrado**, contra `ECU-04 FA-03`, que dice «El Sistema no suprime nada». El diagnóstico de `SD-39` era correcto; el remedio cambió un callejón sin salida por una **caída al éxito**. `H-02` cae por la misma causa. **`H-14`, en cambio, quedó confirmado.**

   > **Lo que la regla #2 aportó al ejecutarse por fin, y que ningún validador había visto:** **20 de las 43 clases** se emitían con nombre que **no es un identificador válido**, porque el generador toma la etiqueta y no el alias. Las 20 tienen alias válido — el defecto es del generador, no del modelo. Es además el argumento retroactivo de por qué `H-20`, la unificación de alias, nunca fue cosmética.

   *Lo que sigue, para contexto de cómo se llegó aquí:*

   > **La ampliación a cobertura total empeoró el veredicto, y estaba previsto que pudiera.** La primera pasada cubrió 6 de 14 especificaciones y emparejó 4 de 72 obligaciones; la segunda cerró ambos huecos —**14/14 y 168/168**— y encontró **7 hallazgos más**, uno Mayor. La tasa subió de 0,37 a **0,57 por página** frente a la base medida de 0,4: no es que el diseño empeorara, es que **la cobertura parcial escondía hallazgos**. Dar por bueno lo que costó esfuerzo es justo lo que la escala de IEEE 1028 no admite negociar.

   > **Los tres mayores, que son los que bloquean el paso al código.** **`H-01`:** **dieciocho** fragmentos `break` sobre flujos cuyo texto declara que se vuelve — `break` abandona el fragmento envolvente en UML, así que el diseño construye un callejón sin salida donde la especificación pide reintento. El peor es el *timeout* de 20 s de `DS-06:105`; y seis forman un sub-patrón propio, los flujos de **cancelación** que «cancelan y vuelven al paso 1». Se corrige sustituyendo por `opt`. **`H-02`:** la atomicidad que `ECU-04 RE-04` promete se realiza con una auto-llamada `deshacerLoSuprimido()` que **ningún mecanismo puede implementar** sobre DynamoDB sin respaldos (`ADR-002-D5` + `ADR-003`). Se cierra **reabriendo `ECU-04`**: la promesa pasa de «atómico todo-o-nada» a **borrado ordenado, tolerante a fallo parcial y reintentable**. **`H-14` (nuevo):** **la auditoría del reinicio de caracterización no existe** — `ECU-11` exige que quede «fechado y auditable», y `DS-11` no declara ningún participante de auditoría ni ningún mensaje que lo registre. Se borra la `CapsulaDePerfil` entera **sin dejar traza**, mientras `DS-10:53` sí audita el *kill switch*.

   > **La reinspección es acotada:** modo *Verificar retrabajo* de `iconix-cdr-review` sobre los artefactos tocados (`DS-04/05/06/10/11/12`, `ECU-04`, `MC-01`, `MC-00`, `TRZ-01`). Las diez reglas ya corrieron y sus resultados están en `CDR-01`; no procede repetirlas. **Y quien verifica el retrabajo no puede ser quien lo hizo** (IEEE 1028 §6.5.6.5).

   > **Después va `ARQ-01`**, el diseño físico: claves de DynamoDB, tabla de *endpoints*, inventario de S3, IAM y red. El motivo lo escribe `ADR-002 §1` sin rodeos: *«el diagrama de clases aún puede mover atributos y operaciones. Fijar claves antes de esa convergencia produce retrabajo garantizado.»* `ARQ-01` hereda cuatro deudas: **CSRF**, el ***endpoint* de cierre de sesión** que reabre `RA-01` de `ECU-03`, **`PER-H4`** —campos y llave de `ContadorDeUsoDiario`— y ahora **`H-09` del CDR**: los códigos de estado que las `ECU` comprometen (400/401/403/409/429/504) no los transporta ningún artefacto de diseño. *(Ni `PER-H5` ni `PER-H2` están ya en esta lista: `ADR-003` y `ADR-004` los cerraron **antes** de `ARQ-01`, porque resultaron ser decisiones sobre qué hace el sistema y no diseño físico. `ARQ-01` hereda de ellos una **restricción** —borrado físico, sin marca de baja, sin respaldo—, no una pregunta. Con las dos cerradas, **`RF-24` pasa a cumplirse según el diseño**; la inmediatez solo se verifica contra una implementación, y eso es fase 4.)*

   > **Lo que el CDR confirmó que está bien**, y conviene no perder de vista: 150/150 controladores con caso de prueba · los 8 conteos canónicos exactos contra los `.puml` · las 16 pantallas de `DIS-00` con clase frontera · **27/27** clases de solución declaradas y justificadas —eran 21 cuando el acta lo escribió; `H-04` sumó los 6 tipos de transferencia y la verificación independiente lo reconfirmó sobre las 27— · 26/26 RF con caso de uso, secuencia y pruebas · cero patrones prematuros · cero tipos vagos. La tasa fue de **0,37 hallazgos por página** contra la base de 0,4: la revisión rindió lo esperado, ni se quedó corta ni el paquete resultó inmaduro.

   > **Los SVG de robustez ya están regenerados** (`SD-31`): los catorce pasan el pase geométrico en cero colisiones. `MD-01.svg` y `DCU-01.svg` **no entraban en ese trabajo** y no hay nada que regenerar en ellos — los produce **PlantUML**, no el generador propio, y sus `.puml` no cambian desde antes de `SD-30`. Una nota anterior decía lo contrario; era un error, corregido aquí y en `DS-00 §10`.

2. **Los pendientes declarados** viven en su propia tabla, más abajo: **37 filas — 27 cerradas, 9 abiertas y 1 declarada sin acción inmediata**, contadas a máquina. **`PER-H5`, `PER-H2` y `COD-01` están cerrados** (SD-33, SD-35 y SD-32), y con ellos `RF-24` pasa a cumplirse según el diseño. **De las diez abiertas, solo la 30 bloquea el paso al código**; tres son de `ARQ-01` (4, 5, 7), una de la fase de construcción (6), dos de las fases D.5 y D.6 (8, 9), una del dueño de `VIS-01` (23), una del usuario (11) y una de la entrega —el informe académico, **pospuesto por decisión** hasta después de la fase 5— (10).
3. **Propagación pendiente de `RPD-01`** (no bloquea, pero está declarada): dos hallazgos menores de la compuerta quedaron abiertos sin acción propia — dos defectos documentados en el validador `validate_pdr.py` de la skill `iconix-pdr-review` (fuera del alcance de este subproyecto: se reportan para quien mantenga la skill) y la guía #4 (participación técnica y no técnica), que exige una sesión con más de un rol y no se pudo ejercer en esta pasada.
4. **Informe académico** (`docs/09_informe/Informe_Academico_Alan_Aura.docx`): sigue reflejando el estado anterior a `PDR-01`/`RPD-01` **y además describe el stack anterior** — se comprobó que contiene menciones vivas de Django, SQLite y PythonAnywhere, superados por `ADR-002`. Es el único artefacto que el profesor lee de principio a fin, y **ningún validador lo cubre**: `verificar_coherencia.py` solo recorre archivos `.md`, así que el `.docx`, los *mockups* `.html` y los `.puml` quedan fuera del barrido por diseño. **Pendiente de actualizar antes de la próxima entrega**, ahora por dos motivos en vez de uno.
5. Revisión del usuario de VIS-01/ADR-001/REQ-01 (validaciones nivel 6: servicios externos y frontera legal) — sigue abierta desde la Fase 1.
6. **Diseño de interfaz (SD-23, cerrado):** base visual producida — `docs/08_diseno/DIS-00` (inventario de 16 pantallas + plan) y `DIS-01` (sistema de diseño con contraste AA, doble voz Alan/Aura, semilla teal/ámbar), más **16 mockups de alta fidelidad** (claro/oscuro, estados no-felices, contención). `PDR-01` reasignó tres pantallas a los casos de uso nuevos (P-13 → CU-04/11/12, P-09 → CU-14, P-10 → +CU-13); el resto del inventario sigue vigente.
7. **Mapa de persistencia (SD-25, cerrado):** `docs/03_requisitos/PER-01_mapa_persistencia.md` consolida qué se guarda en la BD, qué nunca se persiste y las reglas transversales del esquema (`PER-T1…T7`). `PDR-01` le añadió la tabla de correspondencia nombre de persistencia ↔ clase del dominio y reparó las citas que la renumeración de casos de uso rompió. **Insumo directo del modelo de datos**, que sigue fuera de alcance.
8. **Hallazgos de persistencia:** `PER-H1` y `PER-H3` (cerrados en SD-26, antes de robustez). **`PER-H5` cerrado en SD-33** (`ADR-003`: el almacén operativo no se respalda) y **`PER-H2` cerrado en SD-35** (`ADR-004-D1`: la supresión es física e inmediata, sin marca de baja — la ambigüedad era sintáctica y el MVP no tiene «cierre de cuenta»). Con las dos, **`RF-24` pasa a cumplirse**. `PER-H4` (campos de `ContadorDeUsoDiario`) sigue abierto, no bloquea. Detalle histórico del cerrado, para quien lo busque: `ADR-002-D6` había puesto los respaldos en S3 y ese respaldo escapaba a la cascada; se resolvió en `ARQ-01` **antes de cualquier uso con personas reales**, junto con `V6-b`.
9. **`AccionAdministrativa`** permanece deliberadamente fuera de `MD-01` (auditoría de operación, no concepto del problema) — decisión reafirmada en `RPD-01` H-02, marcada en `DR-10` para que siga siendo discutible.
10. Diferido explícito: repo git separado (✅ ejecutado, SD-18).
11. **Grafo de conocimiento propio del subproyecto (SD-27, cerrado 2026-07-25):** `grafo/` — 2.876 nodos, vault de Obsidian trazable. **No se ha regenerado** desde entonces, y el desfase ya es grande: no incluye `MD-01 v1.6`, `DCU-01 v2.2`, las 14 ECU, los 14 DR, `PDR-01`/`RET-01`/`RPD-01`, `ADR-002` ni **nada del paquete de secuencia** (`DS-00…14`, `DOP-01`, `CP-00…14`, `TRZ-DS-01`). Instructivo de uso: `GUIA_USO_GRAFO_Y_VAULT.md` (raíz).

## Pendientes declarados

**Por qué existe esta tabla.** Los pendientes estaban **declarados pero dispersos**: `PER-H5` se
menciona en **dieciocho** archivos y `COD-01` en **seis**, sin ningún sitio donde estuvieran todos juntos.
Quien rehidrataba contexto tenía que reconstruir la lista a base de `grep`, y un pendiente que hay
que buscar es un pendiente que se olvida. Aquí están las treinta, con quién los cierra y qué bloquean.

**Ninguno es un descubrimiento nuevo**: todos venían declarados en su artefacto. Lo que cambia es
que ahora se leen de una vez.

| # | Pendiente | Qué está abierto | Lo cierra | Qué bloquea | Detalle en |
|---|---|---|---|---|---|
| 1 | ~~**`PER-H5`**~~ | ✅ **CERRADO en SD-33** por [`ADR-003`](../01_vision/ADR-003_no_respaldo_del_dato_personal.md), y **antes de `ARQ-01`**: resultó no ser diseño físico sino un **no-objetivo declarado**. El almacén operativo **no se respalda**, así que no hay copia que escape a la cascada de `PER-T1`. Se cerró **quitando el segundo almacén**, no acotándolo | — | Nada. `RF-24` pasa de **dos excepciones abiertas a una** | `ADR-003`, `PER-01 §8` |
| 2 | ~~`COD-01`~~ | ✅ **CERRADO en SD-32.** `docs/07_casos_uso/clases/COD-01_insumos_para_codigo.md`, con las **43** clases en 6 capas de diseño y las **201** operaciones con firma completa *(37 en 5 capas hasta SD-39, que añadió la capa de tipos de transferencia)*. La objeción de las «dos fuentes en conflicto» se neutraliza por construcción: `COD-01` se declara **proyección tabular de `MC-01`**, no fuente propia, y el CDR sigue generando las cabeceras desde `MC-01` | — | — | `COD-01` |
| 3 | ~~`PER-H2`~~ | ✅ **CERRADO en SD-35** por `ADR-004-D1`: la supresión es física e inmediata, sin marca de baja. Era una ambigüedad sintáctica del plan §4.14, no un hueco de diseño | — | Nada. **`RF-24` pasa a cumplirse** | `ADR-004`, `PER-01 §8` |
| 4 | `PER-H4` | Campos y granularidad de `ContadorDeUsoDiario`, sin especificar en ningún artefacto | `ARQ-01` | — | `PER-01 §3.5`, `ECU-09 RA-01` |
| 5 | `RA-01` de `ECU-03` | El cierre de sesión no tiene interfaz declarada, y `ADR-002` lo reabre: borrar una cookie `httpOnly` exige respuesta del servidor, así que probablemente vuelva a hacer falta un *endpoint*. **Las dos fuentes no coinciden en quién lo cierra** — `ECU-03 RA-01` dice «fase de construcción»; `ADR-002 §3` fila 3, posterior, dice `ARQ-01`. Manda `ADR-002`, por ser la decisión de arquitectura vigente | `ARQ-01` | — | `ECU-03 §13`, `ADR-002 §3` fila 3 |
| 6 | `RA-01` de `ECU-14` | El paso que persiste `character` **no tiene *endpoint* visible declarado** en ninguna especificación | **Fase de construcción** — así lo declara `ECU-14`, y **no** `ARQ-01`: no es diseño físico, es una ruta que nadie llegó a nombrar | No bloquea la especificación | `ECU-14 §13` |
| 7 | **CSRF** | Frontend y backend dejan de compartir origen, lo que impide `SameSite=Strict` y **obliga a construir la protección que Django daba de fábrica** | `ARQ-01` | — | `ADR-002 §3` fila 1 |
| 8 | Dos de `HECHOS_CANONICOS` | ~~(a) El **registro** del pendiente de «1.500 caracteres» sigue abierto~~ **— CERRADO en `SD-48`.** *(Cita histórica del valor retirado; el vigente es **2.500** y lo dicen ya `RF-25` y `MV-01`.)* La afirmación falsa vivía en `ECU-00 §7` y se retiró; (b) la matriz de clases candidatas de `MV-01` sigue en **12** mientras `MD-01` tiene **16** | Fase **D.5** | El (b) es un hueco de propagación del `PDR-01`, no un valor histórico | `HECHOS_CANONICOS §Estado de los pendientes` |
| 9 | Propagación de `RPD-01` | Dos hallazgos menores de la compuerta, sin acción propia | Fase **D.6** | No bloquea | `RPD-01` |
| 10 | Informe académico | `docs/09_informe/` refleja el estado **anterior a `PDR-01`/`RPD-01`** y describe **el stack anterior** a `ADR-002` | Pasada de actualización | La entrega | `docs/09_informe/` |
| 12 | ~~**`.svg` de `MC-01`**~~ | ✅ **CERRADO en SD-39.** Generado **y mirado**: PlantUML 1.2026.6 —la misma versión que produjo `MD-01.svg`—, `9347 × 2208 px`, las 54 declaraciones dibujadas. **La cautela de aplazarlo resultó acertada:** cuatro hallazgos del CDR tocaban `MC-01`, y encima el retrabajo le añadió 6 clases y 9 relaciones. **La inspección visual dio tres hallazgos que ninguna comprobación estructural podía dar** —falta de título, proporción 5:1 y los tipos de transferencia sin relación—, dos de ellos ya corregidos. **Y la primera rasterización salió cortada** (tope de 4096 px sobre un diagrama de 10918), el mismo modo de fallo que `PDR-01 §7` documenta: se repitió con el tope elevado | — | `MC-00 §4.2` | `MC-00 §4.2`, `CDR-01 §11` |
| 11 | Revisión de nivel 6 | Validación del usuario sobre `VIS-01`/`ADR-001`/`REQ-01`: servicios externos y frontera legal | El usuario | — | Abierta desde la Fase 1 |
| 13 | ~~**Retrabajo del CDR**~~ | ✅ **APLICADO en SD-39** — no verificado, ver el 15. De los **20** hallazgos de la v1.1: **17 aplicados**, `H-03` diferido con riesgo aceptado, `H-09`/`H-19` heredados por `ARQ-01`. Artefacto por artefacto y **con la skill dueña de cada uno**. **El propio retrabajo destapó 8 hallazgos nuevos** (`H-21`…`H-28`), de los que dos son Moderados: el punto ciego del verificador (fila 16) y el de la regla #2 (fila 14). **Tres correcciones al acta:** `H-14` era `RE-06` y no `RE-05`, `H-16` era `CA-10` y no `CA-12`, y el reparto de `H-01` es **33 correctos + 17 sustituidos**, no «26 y 18» — esa cifra se equivocó **tres veces** por confiar en heurísticas en vez de releer la fila de la `ECU` | — | `CDR-01 §8-ter`, `§8-quater` |
| 14 | ~~**Regla #2 del CDR**~~ | ✅ **CERRADA en SD-39**, y fue la más rentable de las diez. `H-04` era prerrequisito y se cumplió, así que **no salió ningún `???`**. Pero destapó lo que **ninguno de los ocho validadores había visto**: **20 de las 43 clases se emitían con nombre que no es un identificador válido**, porque el generador de la skill toma la **etiqueta** y no el **alias**. Las 20 tienen alias válido — defecto del generador, no del modelo. Resuelto con herramienta **propia y versionada** (`scripts/generar_cabeceras_mc01.py`), con sus tres guardas probadas. **`MC-01_cabeceras.txt` se versiona**, y para que no se desincronice `verificar_coherencia.py` gana el **bloque 5** de frescura | — | `MC-00 §5.4`, `CDR-01 §8-quater H-26` |
| 15 | ~~**Verificación independiente del retrabajo**~~ | ✅ **CERRADA en SD-40**, y mejor de lo previsto: se esperaba otra sesión del **mismo** modelo y la hizo **otro modelo** —**OpenAI Codex `gpt-5.6-sol`**, con encuadre adversarial—, que no comparte los puntos ciegos de quien aplicó. Emitió **`CDR-01 v1.4`**. Confirmó 22 de los 28 objetos y **refutó `H-01` y `H-02`** | — | **Ya no bloquea por falta de verificación; ahora bloquea por sus hallazgos** (filas 19–21) | `CDR-01 §8-quinquies`, §13 |
| 19 | ~~**`VI-01` — las ramas cuyo texto dice «Vuelve» y el diagrama no devolvía**~~ | ✅ **CERRADO en SD-44, al CUARTO intento y por fin atacando la causa.** `SD-43` lo dio por cerrado con `loop` + `alt` y la **tercera** verificación refutó **8 de 13** (`TVI-01`, `TVI-02`): el operando de éxito quedaba **vacío** y la continuación **fuera** del `alt`, así que el sufijo de éxito se ejecutaba igual — el defecto de `opt` con otro nombre. `SD-44` fija la convención (R1–R4), la instala como comprobador y la prueba haciéndola fallar | — | Ya no bloquea | `SD-44`, `barrido_desenlaces.py`, `CDR-01 §8-septies` |
| 20 | ~~**`VI-02` — la nueva semántica no se propagó por la costura**~~ | ✅ **CERRADO en SD-41.** Costura `DR→DOP→COD→CP` repetida con la skill dueña de cada capa. Robustez por **sustitución 1:1**, así que **262 elementos y 150 controladores no se movieron**. Destapó dos cosas que el acta no había visto: la nota de `DR-04` seguía declarando el borrado **ATÓMICO** —la promesa exacta que `H-02` derribó— y `COD-01` proyectaba `revocarCapaBase()` **sin el parámetro** que `H-11` le añadió | — | Ya no bloquea | `CDR-01 §8-quinquies` |
| 21 | ~~**`VI-03` — `ECU-08` conserva tres sobrepromesas**~~ | ✅ **CERRADO en SD-41.** Y eran **cinco**, no tres: el barrido por concepto encontró dos que el acta no listó — `RA-04` descartaba el buscador invocando «la reidentificación que `RE-04` evita», y el checklist #20 apelaba a un «canon anti-reidentificación». Ambas se apoyaban en la promesa que `H-17` retiró. **`RE-04` y las cinco columnas de `RF-15` intactas**: era alinear lenguaje, no recortar alcance | — | Ya no bloquea | `ECU-08 v2.2`, `CDR-01 §8-quinquies` |
| 23 | **`RA-07` de `ECU-08` — la sobrepromesa sigue viva en `VIS-01`** | Menor. `OBJ-6` (`VIS-01 §3.2`) dice «sin acceder a contenido sensible **ni a datos individuales**», que es la formulación incumplible que `H-17` retiró de `ECU-08` — y el **§Alcance del propio `VIS-01`** dice «sin acceder a datos **sensibles**», así que el artefacto se contradice. `RF-15` **exige** mostrar filas individuales | El dueño de `VIS-01`, con su cadena de propagación (`REQ-01`, `TRZ-01`, `MV-01 §7.4`) | Nada hoy: `ECU-08` cita literal y añade la lectura correcta | `ECU-08 §13 RA-07` |
| 24 | ~~**`SVI-02` — las dos exenciones del verificador amplían de más**~~ | ✅ **CERRADO en SD-43, probado haciéndolo fallar.** `SD-42` eximió la línea `**Insumos:**` **entera** y el archivo `CDR-01` **entero**. Ahora se exime solo la cita versionada —`ARTEFACTO vX.Y (N unidad)`— y solo las **secciones históricas** del acta. Sabotaje idéntico sobre copias: con `SD-42` daba «ninguno»; con `SD-43` caza **tres** | — | — | `verificar_coherencia.py` |
| 25 | ~~**`SVI-03` — la cifra de la convención de orden**~~ | ✅ **CERRADO en SD-43.** `SD-42` publicó «23 descendentes / 3 / 5»; reproducido contra `239c019` da **21 / 3 / 7**. El fallo fue **de método**: se midió *después* de reparar ya `PER-01` y `CP-00`, y se presentó como el estado previo. Corregido en `SD-42`, en el `CHANGELOG` y en el comentario del bloque 6 | — | — | `REGISTRO_DECISIONES SD-42` |
| 26 | ~~**`SD-43-H1` / `TVI-04` — los `break` dentro del `loop` de `DS-06`**~~ | ✅ **CERRADO en SD-44.** Confirmado por la tercera verificación en sustancia y **corregido en su recuento**: son **tres** `break` internos, no seis. **No se movieron los `break`**, que dentro del `loop` expresan bien la terminación; lo que estaba mal era que tras el bucle quedara «cerrar la conversacion» —acción del **curso básico**— ejecutándose después de que la `ECU` dijera «Termina». Un `opt` la acota y deja `cerrar()` y `descartarContenido()` como **limpieza común** | — | Ya no bloquea | `DS-06 v1.3` |
| 27 | ~~**`TVI-01` — el sufijo de éxito fuera del `alt`**~~ | ✅ **CERRADO en SD-44 (regla 1).** `DS-04 FE-04` confirmaba «no queda dato recuperable» tras una cascada a medias; `DS-11 FE-04` declaraba el chat inhabilitado y ofrecía rehacer **una caracterización intacta**. Los pasos 4 y 5 pasan **dentro** del operando de éxito. El barrido dio exactamente estas 2 instancias, ni una más | — | — | `DS-04 v1.4`, `DS-11 v1.4` |
| 28 | ~~**`TVI-02` — un `loop` no puede realizar dos retornos distintos**~~ | ✅ **CERRADO en SD-44 por disolución, no por parche (regla 3).** Un `DS` no puede expresar dos puntos de reentrada con un solo `loop`, y forzarlo es el anti-patrón que la propia *skill* prohíbe. El punto exacto se traslada donde **sí** es verificable: **cinco `CP`** lo fijan, y **dos —`CP-1109` y `CP-1207`— decían «paso 1» cuando su `ECU` dice «paso 2»** | — | — | `CP-03/04/10/11/12`, notas de `DS-10`/`DS-12` |
| 29 | ~~**`TVI-05` y `TVI-06` — recuentos publicados como hecho**~~ | ✅ **CERRADOS en SD-44.** «22 `break` legítimos» eran **16** en los seis diagramas tocados; «seis dentro del `loop` de `DS-06`» eran **tres**. Y `TVI-06` volvió a quedarse corto: el acta listaba **dos** historiales con `23/3/5` y el barrido por concepto halló **ocho**. Tachados y rectificados, nunca borrados (`SD-31`) | — | — | 8 historiales + `CHANGELOG` |
| 30 | ~~**Cuarta verificación independiente de `SD-44`**~~ | ✅ **EJECUTADA (`CDR-01 v1.7`, Codex GPT-5).** Confirmó que `SD-44` **es un replanteo real, no un parche disfrazado**, y cerrados `TVI-01`, `TVI-03`, `TVI-04`, `R4`, `TVI-05` y `TVI-06`. Pero **refutó `R3`** (`CVI-03`, Mayor) y encontró tres Moderados en el instrumental. Todo ello se corrige en `SD-45` | — | Ya no bloquea por falta de verificación | `CDR-01 §8-octies` |
| 31 | ~~**`CVI-03` — `R3` estaba afirmada, no implementada**~~ | ✅ **CERRADO en SD-45.** *«Dibujar una reentrada y declarar en una nota que no se pretende dibujarla no elimina la semántica del fragmento»*. Los cuatro `loop` de confirmación se acotan **al paso 2**, que es adonde manda su `ECU`, y la **cancelación sale del bucle**: nunca fue una iteración. `DS-06` no se toca —su `loop 1..20` es iteración genuina— y se declara por qué. Seis `CP` alineados | — | — | `DS-04/10/11/12 v1.5`, `DS-06 v1.4`, 6 `CP` |
| 32 | ~~**`CVI-01` y `CVI-02` — el comprobador decoraba la convención**~~ | ✅ **CERRADOS en SD-45.** Catorce sabotajes tumbaron el barrido de `SD-44`: cualquier fragmento ocultaba una violación de R2, tres formas de flecha escapaban al regex, una nota con la palabra `end` descuadraba el recorrido y `group` daba falsos positivos. Reescrito, con **17 fixtures versionados** y `--autoprueba`: **17/17**. Y la ruta absoluta —que violaba la **regla de oro** de `AGENTS.md §0`— se resuelve desde `__file__` | — | — | `barrido_desenlaces.py`, `scripts/fixtures/` |
| 33 | ~~**`CVI-04` — el `plantuml-src` embebido del `.svg` de `MC-01`**~~ | ✅ **CERRADO en SD-46, y sin PlantUML.** El bloque resultó ser la fuente comprimida con **DEFLATE crudo** y codificada con el **base64 propio de PlantUML**, así que se pudo decodificar, aplicarle los dos renombres de `SD-44` y `SD-45` y volver a codificarla. Comprobado **por ida y vuelta antes de escribir** —decodificar lo escrito devuelve exactamente la fuente esperada— y contra el `.puml` vigente: **coinciden**. El artefacto vuelve a ser fiel a su propia fuente y regenerable desde su metadato | — | — | `MC-01_modelo_clases_diseno.svg` |
| 34 | ~~**`ECU-06 FE-07` — pedía volver al paso 4 sin declarar mecanismo**~~ | ✅ **CERRADO en SD-46 por `/use-case-specifier`, su skill dueña.** `FE-07` pasa a **volver al paso 2** y se hace explícito el mecanismo que sí existe: la interfaz queda viva y el Usuario reenvía. **Lo confirma `CA-06`** —«permite reintento sin romperse»: el reintento *del Usuario por la interfaz*, no el automático que solo `FE-06` declara— y lo confirma el diseño: un reintento automático tras 20 s **duplicaría la espera que `RN-02.9` acota**. Barridas las **once** filas `FA`/`FE`: era la única. Propagado a `CP-023` y a la nota de `DS-06`; **181 casos de prueba, sin mover** | — | — | `ECU-06 v2.2`, `CP-06 v1.3`, `DS-06 v1.5` |
| 35 | ~~**El veredicto del `CDR-01`**~~ | ✅ **DETERMINADO el 2026-08-05 por el líder del proyecto** (`IEEE 1028 §5.2.1`; el acta **propone**, el líder **determina**): **`ACEPTADO CON VERIFICACIÓN DE RETRABAJO`**. Cero Críticos y **cero Mayores** abiertos. Los Moderados que sostenían la condición —`CVI-04` y `ECU-06 FE-07`— quedaron **cerrados** en `SD-46`, no diferidos. **La compuerta entre el diseño detallado y el código queda cerrada** | — | — | `CDR-01 §13`, `SD-46` |
| 36 | ~~**`VRI-01` — las notas no convergieron**~~ | ✅ **CERRADO en SD-47.** La conducta era correcta; el texto se quedó atrás. `DS-06` seguía declarando **abierta** la discrepancia de `FE-07`, que `SD-46` cerró (`ECU-06 v2.2`: vuelve al paso 2), y `DS-12` declaraba una «aproximación» que `SD-45` ya había **eliminado** al sacar `FA-03` del bucle — contradiciendo a la nota de al lado | — | — | `DS-06 v1.6`, `DS-12 v1.7` |
| 37 | ~~**`VRI-02` — falso negativo nuevo del comprobador**~~ | ✅ **CERRADO en SD-47, y versionado como fixture.** Reconocía solo emisores con el prefijo `ACT_`, pero PlantUML admite `actor "Usuario adulto"` **sin alias**, citado luego con comillas y espacios: el validador de secuencia lo acepta en 0/0 y el barrido daba `R2 = 0`. Ahora los emisores de actor se **derivan de las declaraciones `actor` del propio diagrama**. **18/18** en autoprueba. Su frase queda escrita en el script: *el 17/17 demostraba regresión sobre los defectos conocidos, no suficiencia general* | — | — | `barrido_desenlaces.py`, `fixtures/actor_citado_con_espacios__r2-1.puml` |
| 22 | ~~**`VI-07`, `VI-08` y `SD-40-H1` — tres huecos del instrumental**~~ | ✅ **CERRADOS en SD-42, los tres probados haciéndolos fallar.** **`VI-08`:** `VALORES_OBSOLETOS` pasa de vigilar **4 hechos de 29** a vigilar **9**; entran los cinco que `SD-39` movió sin declarar obsoleto ninguno, que es la causa directa de que `SD-40` tuviera que barrer once puntos a mano. `H-28` **no entra, y se declara por qué**: 21 y 27 son ambos correctos según la capa, y un regex no puede razonar eso. **`VI-07`:** entra `--base <ref>`, así que los bloques 3 y 5 pueden auditar una rama comiteada; y su rótulo dice ahora contra qué comparan, para que un verde no se lea más ancho de lo que es. **`SD-40-H1`:** el alias se captura entero —`([^\s{]+)` en vez de `(\w+)`—, así que la **segunda guarda deja de ser inalcanzable**: antes emitía `B_Landing` donde el modelo decía `B_Landing-P01`, en silencio. Verificado: 0 cambios sobre el `MC-01` real | — | — | `verificar_coherencia.py`, `generar_cabeceras_mc01.py` |
| 16 | ~~**Punto ciego del bloque 4** (`H-24`)~~ | ✅ **CERRADO en SD-42.** El patrón de ID admite ahora IDs **compuestos**, así que `TRZ-DS-01` deja de leerse como `DS-01`. **Probado haciéndolo fallar**: con la versión anterior el mismo sabotaje daba «ninguno». Hubo que corregir esa versión **a mano dos veces** —`SD-40` y `SD-41`— porque el bloque no podía verla | — | — | `verificar_coherencia.py` |
| 17 | **Tres defectos del generador de la skill** | `H-M` y `H-M2` (validador de clases) más el nuevo: el generador de cabeceras usa la etiqueta en vez del alias, y nombra los campos de relación concatenando sin *camelCase* (`sedocumentaconEventoDeSeguridad`). **Ajenos a este repositorio** | El mantenedor de `iconix-cdr-review` / `uml-design-class-model` | Nada: los tres tienen rodeo propio y versionado aquí | `MC-00 §6`, `§5.4` |
| 18 | ~~**Nota de versión de `MD-01.md` y `DCU-01.md`**~~ | ✅ **CERRADA en SD-42**, la pasada de gobernanza que la fila pedía. `MD-01.md` sube a **v1.8** y su historial queda registrado y **ordenado** —iba v1.6 sobre v1.7—. `DCU-01.md` **no se toca, y se dice por qué**: no tiene tabla de historial en absoluto, así que no hay fila que añadir; el `title` de su `.puml` está justificado dentro del propio `.puml`, que es donde vive el cambio | — | — | `MD-01.md v1.8` |

> **Actualizado en SD-40.** Siguen cerrados los tres que llevaban marca: **`PER-H5`** (fila 1, `ADR-003` en
> SD-33 — el almacén operativo no se respalda), **`PER-H2`** (fila 3, `ADR-004-D1` en SD-35 — la supresión es
> física e inmediata) y **`COD-01`** (fila 2, SD-32). Con los dos primeros, **`RF-24` se cumple** — con la
> precisión que `TRZ-01 §2` no deja perder: **según el diseño**, porque la inmediatez solo se puede verificar
> contra una implementación, y eso es fase 4.
>
> **El recuento, contado a máquina y no a ojo** —a ojo ya falló una vez—. La tabla tiene **37** filas:
> **27 cerradas** (1–3, 12–16, 18–22, 24–35 y las nuevas **36 y 37**), **9 abiertas** (4–11 y **23**)
> y **1 declarada sin acción inmediata** (17, que es de la skill y no de este repositorio). Suman 37.
> **Ninguna abierta bloquea el paso al código**, y **ninguna es del `CDR`**: tres van a `ARQ-01`, una a
> construcción, dos a las fases D.5 y D.6, una al dueño de `VIS-01`, una al usuario y una a la
> entrega del informe.
>
> **Y la fila 19 cambió de estado TRES veces**, que es lo más honesto que dice este tablero: `SD-41` la
> cerró con una lectura falsa, la segunda verificación independiente la **reabrió** (`SVI-01`, Mayor),
> `SD-43` la volvió a cerrar y la **tercera** la reabrió otra vez (`TVI-01`/`TVI-02`); la cierra `SD-44`,
> al cuarto intento y atacando la causa. **Ocho de las diez abiertas son heredadas** —`ARQ-01`,
> construcción, informe, validación del usuario—; las dos propias son la **23** (`VIS-01`) y la **30**,
> que es nueva y la única que bloquea el paso al código.
>
> **`SD-42` cerró las tres del instrumental de golpe** —16, 18 y 22— y con ellas la categoría entera de
> «declaradas sin acción inmediata» se queda en una sola fila, que además es ajena. *(La nota de `SD-37` decía «once abiertos» y
> atribuía los «dos nuevos» a las filas 13 y 14, que ya estaban cerradas: el recuento llevaba dos versiones
> sin cuadrar, y lo cazó el barrido de `SD-40`.)*
>
> **Los tres hallazgos de diseño del CDR están cerrados** (`SD-41`): los dos **Mayores** —`VI-01` y `VI-02`, los
> que tocaban **comportamiento**— y el Moderado `VI-03`. Queda el instrumental (fila 22) y el hallazgo que
> `VI-03` destapó hacia fuera (fila 23, `VIS-01`).
>
> **Las tres fases coincidieron en algo, y conviene leerlo junto:** el acta se quedó **corta en las tres**.
> `VI-01` listaba diecisiete ramas y solo cuatro estaban mal; `VI-02` señalaba controladores y el defecto estaba
> también en una nota y en una firma; `VI-03` listaba tres sobrepromesas y eran **cinco**. No es un reproche a
> la revisión —encontró lo que ninguna sesión del autor iba a encontrar—: es que **una lista de ubicaciones es
> una pista, no un inventario**, y cada fase se cerró barriendo por concepto en vez de por número de línea.
>
> **Lo que falta para el veredicto no es corregir más, es volver a verificar.** El paquete no puede
> autodeclararse corregido: `IEEE 1028 §6.5.6.5` pide **verificador ≠ autor**, y el retrabajo de `SD-41` lo
> aplicó **Claude Opus**, el mismo que aplicó `SD-39`. La re-verificación vuelve a **Codex `gpt-5.6-sol`**,
> que es el ciclo que esta sesión ya validó. **El veredicto lo determina el líder**, no la revisión.

## Invariantes que no se relajan
- **Independencia** (SD-18): el subproyecto no depende del macroproyecto; toda mención a él es cita de procedencia histórica, nunca una referencia funcional viva.
- Canon de dominio en cada artefacto; cero requisitos huérfanos; umbral+GQM por requisito de calidad.
- Sin default de modelo de razonamiento; subagentes solo para lo mecánico, auditados por el orquestador antes de incorporarse.
- **Trinquete:** no degradar lo ya construido y verificado. Corregir con evidencia (validador o comando), no por afirmación.
