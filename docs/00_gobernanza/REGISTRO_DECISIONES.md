# Registro de decisiones — Subproyecto «Alan & Aura Académico»
**Propósito:** historial de decisiones con ID, justificación e impacto (rasgo E8-8). **Fecha:** 2026-07-12.
**Convención de ID:** `SD-NN` (decisión del subproyecto). Estado: `confirmada` (usuario) / `de plan` (heredada del plan Codex, re-verificable) / `propuesta` (a validar).

---

| ID | Decisión | Estado | Justificación / Impacto |
|---|---|---|---|
| SD-01 | El subproyecto es un **hijo académico simplificado** de Smart-AID (Releases 0.1+0.2), aislado en `subproyecto_academico_alan_aura/`. | confirmada | Materia con horizonte ~1 mes; recorta módulos, no principios. Impacto: todo artefacto vive aquí; el macro es solo lectura. |
| SD-02 | **Doble horizonte** explícito (académico vs producto); Jonatan es propietario/ideador/líder en ambos. | confirmada | Evita confundir el MVP del curso con el producto profesional. |
| SD-03 | **Sin default de modelo de razonamiento**; lo elige el usuario por sesión. | confirmada | Regla de gobernanza del repo; heredada al subproyecto. |
| SD-04 | Profundidad de modelos verbales = **subconjunto de 11 rasgos de E8**: {1, 2, 3, 6, 7, 12, 13, 14, 15, 16, 17}. Cada MV cierra con checklist verificable. | confirmada (usuario) | Calibración académica: rigor del estándar sin el coste de los 17 rasgos completos. Impacto: define la estructura de MV-01 y submodelos. |
| SD-05 | Seguridad = **gate binario** (peligro explícito → fallback determinista), sin escala S0-S5; declarado con honestidad y **mapeado a S0-S5 del macro como procedencia**. | confirmada (usuario) | Realizable en 1 mes; honesto sobre su alcance ("no es detección exhaustiva"). Impacto: SEG-01. |
| SD-06 | ~~Stack: **Django 5.2 LTS · SQLite · Groq (`gpt-oss-20b`) · GitHub · PythonAnywhere Free** (Render contingencia).~~ **SUPERADA por SD-29** el 2026-08-01, salvo Groq y GitHub, que siguen vigentes. | de plan → **superada** | Del plan Codex; hechos externos re-verificables antes del release ([N5]/[N6]). Impacto: ADR-001, hoy parcialmente superada por `ADR-002`. |
| SD-07 | **No persistencia del chat** en el MVP; el LLM recibe la **cápsula de perfil**, no el historial. | de plan + canon | Minimización y canon de dominio. Impacto: PRIV-01, MV-01 §Conversación. |
| SD-08 | **Solo adultos** con *disclosure* de IA; **menores fuera de alcance**. | de plan + canon | Política de menores pendiente en el macro; no se asume cobertura. Impacto: VIS-01 exclusiones, PRIV-01. |
| SD-09 | Idioma **español (Colombia)**. | de plan | Contexto de la materia y del usuario. |
| SD-10 | **Consolidar** los ~16 IDs del plan Codex en **~9 artefactos** documentales. | propuesta (adoptada) | Evita proliferación; agrupa por afinidad (requisitos, trazabilidad). Impacto: estructura de `docs/`. |
| SD-11 | **Diferir** repo git separado, grafo y vault propios del subproyecto. | confirmada | Fase documental; la carpeta es extraíble después. |
| SD-12 | Recursos de ayuda / líneas de crisis **configurables por entorno**, nunca *hardcodeados* en artefactos. | propuesta (adoptada) | Correctitud operativa y anti-obsolescencia; evita publicar números que cambian. Impacto: SEG-01, ADR-001. |
| SD-13 | **Consolidar** los modelos verbales en **un único MV monolítico** (MV-01 v2.0); los submodelos MV-01.1/.2/.3 se eliminan como archivos y quedan como **vistas internas** del MV. El MV se estructura **grado-extracción** y alineado al *academic strict mode* de la skill `uml-domain-modeler` (sin atributos, **sin multiplicidades**; cardinalidades y dominios de valor a reserva). | confirmada (usuario) | Extracción de dominio limpia desde una fuente única; evita re-coser entidades entre archivos y el anti-patrón "multiplicity too early". Impacto: MV-01, INDICE, REQ/PRIV/SEG/TRZ (referencias → §Vista). Alcance: **no** se genera el `.puml` todavía (Q2). |
| SD-14 | **Generar el modelo de dominio** (`MD-01`, PlantUML) ahora, con la skill `uml-domain-modeler` en modo *academic strict*, a partir de MV-01 + documentos de apoyo. **Reversa de la decisión Q2** (que difería el `.puml`). | confirmada (usuario) | El usuario lo pidió explícitamente. Primer artefacto ICONIX de la Fase 2. Decisión de modelado: `Configuracion` descartada (contenedor técnico), `EventoDeSeguridad`/`RecursoDeAyuda` conservadas. Validador de la skill: 0 errores/0 advertencias. Sigue respetando "no casos de uso" (el dominio precede a los CU). Impacto: nueva carpeta `docs/06_dominio/`. |
| SD-15 | **Corrección de alcance: alinear al plan de Codex.** Se detectó (verificación forense del plan) que al consolidar se **recortaron** funciones reales del administrador (directorio, métricas agregadas, kill switch) y del usuario (registro/login, reinicio de perfil, revocación, eliminación en cascada, manejo de errores), y se **inventaron** funciones que el plan excluye (edición administrativa de recursos/textos/prompts, CMS). Se restaura lo del plan y se quita lo inventado. | confirmada (usuario) | El usuario notó que "faltaban muchas cosas" al administrador. Impacto: VIS-01 (actor Visitante, OBJ-6 redefinido, OBJ-7 nuevo), MV-01 (clase `DisponibilidadDelChatbot`, reglas §7.4/§7.5, `Configuracion` descartada), REQ-01 (admin RF-14…18 realineados, +RF-19…26), PRIV-01 (cuenta, contadores, qué NO ve el admin), CONTRATO (C-9/C-10), MD-01 (12 clases), TRZ-01. Recursos/textos → **por entorno**, no editados por el admin. |
| SD-16 | **Archivar el texto completo (verbatim) del plan de Codex** en `00_PLAN_CODEX_ORIGINAL.md`, como fuente primaria del subproyecto. | confirmada (usuario) | Hasta ahora las citas "plan §X.Y" en los artefactos se apoyaban en una reconstrucción forense parcial (grep sobre el historial). El usuario pegó el texto completo y preguntó si convenía archivarlo — se confirma que sí, seguido del patrón `MANIFIESTO_FUENTES.md`. Impacto: nuevo archivo raíz; INDICE_MAESTRO y MANIFIESTO_FUENTES actualizados. **Hallazgo derivado:** el texto completo reveló detalles más ricos que la reconstrucción forense previa — resuelto en SD-17. |
| SD-17 | **Reconciliar 5 discrepancias** encontradas al comparar los artefactos ya construidos contra el texto completo del plan (SD-16): (1) el historial de sesión **sí** viaja al LLM (hasta 4 intercambios, no "cero historial"); (2) faltaban límites de tasa exactos (3/min, 30/día, 20s timeout, 1.500 caracteres, 350 tokens); (3) inventario de datos de PRIV-01 mapeado 1:1 a las 7 entidades exactas del plan (`User`/`ConsentRecord`/`InitialConversationProfile`/`PlatformSetting`/`DailyUsageCounter`/`OperationalEvent`/`AdministrativeAction`); (4) tabla de personajes del contrato enriquecida (función/tono/respuesta típica/preguntas/sugerencias/longitud/límites, plan §3.6); (5) plantilla del fallback citada del plan §3.8, **sin números fijos** (respeta SD-12: los recursos van por configuración de entorno, no se hardcodean ni siquiera al citar la fuente). | confirmada (usuario) | El usuario pidió reconciliar todos los puntos ahora. Impacto: MV-01 (RN-02.2/RN-03/RN-02.8/RN-02.9), REQ-01 (RF-09/25/26, RNF-04/10, v1.2), PRIV-01 (inventario §2 reescrito, PRIV-R1, v1.2), CONTRATO (C-4, tabla P-1..P-8, v1.1), SEG-01 (§5 plantilla citada, v1.1), TRZ-01 (RF-25/26). |
| SD-18 | **Extraer el subproyecto a este repositorio independiente** (`jonatan8254/alan-aura-academico`, privado) en vez de una rama del repo de SmartAID. Cierra/actualiza SD-11 (que difería la decisión). Ejecutado el 2026-07-12: copia íntegra desde la carpeta `subproyecto_academico_alan_aura/` de SmartAID (nunca comiteada allí), corrección de 11 referencias que apuntaban al macro (ahora citas de procedencia), `git init` + commit único (sin `Co-Authored-By`) y push. | confirmada (usuario) | Una rama comparte descripción, topics, *secrets* de Actions y colaboradores con el repo profesional — mala separación para un producto con stack, licencia y audiencia distintos. El propio plan de Codex ya lo especificaba (§2.1 `alan-aura-academico`, §6.5 condiciones de extracción, §2.6 DoD "repositorio identificable"). **Esta es la fuente de verdad activa**; la copia en SmartAID queda como snapshot histórico archivado. |
| SD-19 | **Adoptar el formato de entrega del curso en MV-01**, anteponiendo una **Parte A** narrativa (secciones 1–6: Título, Contexto, Descripción, RF, RNF, Requisitos de Calidad) al estilo del ejemplo provisto por el usuario (modelo verbal de otro grupo), **conservando** intacta la formalización E8 como **Parte B**. | confirmada (usuario) | El curso exige que el modelo verbal siga esa estructura. Contenido de la Parte A extraído fielmente de VIS-01, REQ-01 y el plan archivado; sin cambios en clases/relaciones/reglas (la extracción de dominio no se ve afectada). Impacto: MV-01 v2.3. **Pendiente de confirmar por el usuario:** el "Período académico 2026-1" del encabezado es una inferencia — ajustar si corresponde. |
| SD-20 | **Producir el diagrama de casos de uso** (`DCU-01`, PlantUML) con la skill `uml-use-case-diagram`, a partir de MV-01, MD-01, VIS-01 y REQ-01. Segundo artefacto ICONIX de la Fase 2. | confirmada (usuario, vía `/uml-use-case-diagram`) | 4 actores (incl. Proveedor LLM como **sistema externo** — coherente con MD-01 decisión 9), 10 casos de uso en 3 paquetes, 1 `<<extend>>` (Derivar ante peligro) y 0 `<<include>>`. Validador de la skill: 0 errores. Los 26 RF de REQ-01 quedan trazados sin huérfanos. Nueva carpeta `docs/07_casos_uso/`. Siguiente: especificación textual de casos de uso (`use-case-specifier`). |
| SD-21 | **Producir la especificación textual de los 10 casos de uso** (`ECU-00…ECU-10`) con la skill `use-case-specifier`. Tercer artefacto ICONIX de la Fase 2. Empaquetado **un archivo por CU + índice**; profundidad **mixta** (completa §1–§23 en CU-04/05/06/07/10; ágil §23 en CU-01/02/03/08/09). Orquestación: los 5 ágiles redactados por subagentes **Sonnet** y auditados por el orquestador; los 5 completos por el orquestador. | confirmada (usuario, vía `/use-case-specifier`) | La skill **no** trae validador ejecutable → verificación **documental**: checklist §22 embebido por spec + loop de auditoría (rúbrica + 8 pasos + traza *backward* + canon §5), sin hallazgos Crítico/Mayor. Los 26 RF quedan trazados sin huérfanos (columna CU de `TRZ-01` poblada). Nueva subcarpeta `docs/07_casos_uso/especificaciones/`. **Hallazgo abierto (RA-01):** discrepancia del nº de campos al LLM — cápsula canónica (3, RN-01.3/PRIV-R1) vs plan §3.4 `ContextoInicialConversacionalV1` (5 + personaje + versión); las specs siguen el canon (3), a reconciliar con el usuario **(→ resuelto en SD-22)**. Siguiente: análisis de robustez. |
| SD-22 | **Reconciliar RA-01: adoptar la cápsula de 5 campos del plan.** El LLM recibe `ContextoInicialConversacionalV1` (plan §3.4) = **5 campos de contenido** (`mood_self_report`, `energy_self_report`, `conversation_goal`, `response_style` autorreportes opcionales; `character` obligatorio {alan, aura}) **+ 2 metadatos** (`schema_version`, `consent_version`), en vez de la cápsula de 3 campos {preferenciaDePersonaje, focoEmocional, tonoPreferido}. | confirmada (usuario) | El usuario resolvió RA-01 a favor del plan original. Propagado con **loop de auditoría hasta converger** a: MV-01 (RN-01.3 raíz, §3, §13.1 — v2.4), REQ-01 (RF-04/05 — v1.3), PRIV-01 (inventario §2, PRIV-R1 — v1.3), CONTRATO (C-4), MD-01 §6, ECU-04/05/06 (§7/§18/§21) y ECU-00 §7; RA-01 **cerrada** en las 3 specs. **Sigue siendo minimización** (autorreportes gruesos de 3–5 puntos, sin historial/diario/biomarcadores; **PRIV-R9 intacta**). Consideración: refuerza la necesidad de la revisión legal V6-b (autorreportes de ánimo/energía). |
| SD-23 | **Producir la base de diseño de interfaz** (`DIS-00` inventario+plan, `DIS-01` sistema de diseño) y **mockups de alta fidelidad** de las 16 pantallas del MVP, fundamentados en evidencia (6 fuentes: alianza terapéutica, color, crisis, consentimiento, tipografía, movimiento) y en el canon. Semilla de arte: **Aura teal / Alan ámbar**, base calma-dominante. | confirmada (usuario, vía prompt de diseño) | Nueva carpeta `docs/08_diseno/` (`DIS-00`, `DIS-01`). Los 16 mockups se renderizaron en la conversación (claro y oscuro, con estados no-felices y la pantalla de contención); los artefactos persistentes son `DIS-00`/`DIS-01`, con **contraste AA verificado**, **doble voz Alan/Aura** y **checklist de canon por pantalla** (no clínico · disclosure previo · sin dark patterns · minimización · admin sin datos individuales · crisis serena). Construible con Django templates + JS mínimo (ADR-001). Base para poblar el §17 de las ECU. **Gap detectado y cerrado en SD-24:** el render en conversación no persiste archivos — los mockups no quedaron en el repositorio hasta SD-24. |
| SD-24 | **Corregir SD-23: persistir los 16 mockups + style-tile como archivos HTML reales** en `docs/08_diseno/mockups/`, más una galería `index.html`. | confirmada (usuario, tras detectar que los mockups no estaban en el repositorio) | El usuario notó que `git status`/GitHub no mostraban los mockups — el render de la Fase 5 (SD-23) solo se había mostrado en la conversación, sin escribirse a disco. Corregido: 19 archivos (`00_style_tile.html`, `p01…p16` con `p10` en variante clara y oscura, `index.html`) autocontenidos — colores hardcoded (mismos valores hex de DIS-01, sin variables CSS de ningún entorno externo), iconos Tabler vía CDN, sin dependencias del proyecto. `DIS-00` (tabla de pantallas) y `DIS-01` (§8) actualizados con enlaces reales a cada archivo. |
| SD-25 | **Consolidar el mapa de persistencia en un artefacto propio** (`PER-01`, `docs/03_requisitos/`): qué se guarda en la base de datos, con qué campos, quién lo ve, cuánto dura y qué nunca se persiste. Declarado explícitamente como **inventario**, **no** como diseño de esquema (sin tipos, claves, índices ni DDL), para no violar la regla de no-adelanto de CLAUDE.md §6. | confirmada (usuario) | El usuario preguntó por el manejo de persistencia del chat y luego pidió el mapa completo de lo que se persiste; la información existía pero estaba **dispersa** entre plan §4.14/§4.15, `PRIV-01` §2 y las 10 ECU, sin un lugar único que la consolidara ni la trazara. Impacto: nuevo `PER-01` (7 entidades con campos trazados, 7 reglas transversales `PER-T1…T7`, mapa de relaciones, frontera de retención del proveedor LLM); `INDICE_MAESTRO`, `ESTADO_PIPELINE`, `README` actualizados. **Aporte nuevo:** primera cita del **plan §4.15** (telemetría) en el corpus — fija los 8 campos de `OperationalEvent` y sus 8 exclusiones, entre ellas el motivo textual del *fallback* y la categoría emocional. **Hallazgos abiertos:** `PER-H1` (contradicción sobre `character` obligatorio — **bloquea el modelo de datos**), `PER-H2` (heredado de RA-01 de ECU-04), `PER-H3` (`estado` del directorio sin dominio de valores), `PER-H4` (menor). **Corrección §4.9:** se retractó por escrito una afirmación falsa del análisis previo («plan §4.14 no existe aquí») — sí existe, en `00_PLAN_CODEX_ORIGINAL.md` (SD-16). |
| SD-26 | **Resolver PER-H1 y PER-H3** (hallazgos abiertos por `PER-01`). **PER-H1:** la `CapsulaDePerfil` **siempre existe** al terminar el onboarding, con `character` como contenido mínimo; `character` se **reclasifica** como *elección de interlocutor* y **precondición funcional** del chat (del mismo rango que el consentimiento), no como autorreporte de perfil. **PER-H3:** el `estado` del directorio administrativo queda acotado a **{activo, sin consentimiento vigente}**, **derivado** de `ConsentRecord` — no editable, no almacenado aparte, no es suspensión. **PER-H2 y PER-H4 se dejan abiertos deliberadamente** (el primero se resuelve en construcción con el hosting a la vista; el segundo es detalle de diseño). | confirmada (usuario) | Se resolvieron **antes** del análisis de robustez para que `DR-04`/`DR-05`/`DR-06` no heredaran la contradicción — corregirla después habría costado el triple. Elegida la opción que **no reabre SD-22**: lo que recibe el LLM (RN-01.3) queda intacto y el contrato `ContextoInicialConversacionalV1` no se toca. Impacto: **RN-01.4 precisada** («ningún **autorreporte** es obligatorio») y **RN-01.6 añadida** en MV-01 v2.5; REQ-01 v1.4 (RF-04/05 con traza a RN-01.6, RF-15 con dominio de `estado`); PRIV-01 v1.4 (inventario §2, PRIV-R10); ECU-05 v1.1 (FA-01, §4, §11 paso 9, §14, §15, §18, §20 CA-05, §21 RA-04, §23); ECU-04 v1.1 (FA-01, §14, CA-02); ECU-08 v1.1 (§2.1, RN-03.2); PER-01 v1.1. **Consecuencia aceptada y documentada:** reiniciar la caracterización (RF-22) borra también `character`, por lo que el usuario queda sin poder conversar hasta rehacer CU-05. **Cardinalidad fijada:** `Usuario–CapsulaDePerfil` = 1 a 1 tras el onboarding. |
| SD-27 | **Incorporar un grafo de conocimiento propio del subproyecto**, en la carpeta nueva `grafo/` — cierra/ejecuta el diferido de SD-11. Construido en el proyecto principal (`SmartAID/grafo_subproyecto/`) sobre **copias**, con extracción vía `graphify extract` por subcarpeta en **modo Opus / `--mode deep`** (un primer intento en bloque dio solo 40 nodos — esencialmente un índice — y se descartó) y nombrado de comunidades con Opus. **Composición:** 2.876 nodos (262 del subproyecto + 2.614 de conocimiento, sin nodos de Smart-AID/TalentTrack), 2.893 aristas, 249 hiperaristas, 769 comunidades (55 nuevas + 714 de conocimiento), 34 puentes `same_as`; las 2.876 notas del vault enlazan a su documento fuente. **Dos decisiones no obvias:** (a) las ~882 figuras extraídas de los libros (~109 MB) **no se duplican** en el vault — pesan más de 10× el texto y son material con derechos, mientras que el texto de los 37 `.md` del subproyecto y los 25 libros/normas sí se copió a `grafo/vault/02_Fuentes/`; (b) `grafo/vault/02_Fuentes/Conocimiento/` (9,8 MB de libros y normas con derechos: PMBOK 7, ISO/IEC 90003, ISO/IEC 25010/25020, Wiegers…) queda **excluida del control de versiones** (`.gitignore`) — el vault es autocontenido en disco, pero el repositorio no redistribuye material licenciado. | confirmada (usuario) | Los artefactos originales del macroproyecto (`graphify-out/`, `vault_obsidian/`) nunca se modificaron — verificado por hash SHA256 antes y después de cada etapa. Al usar `graphify label --missing-only` se detectó que re-clusterizaba **todo** el grafo como efecto secundario (alteraba 2.162 de los 2.614 nodos de conocimiento); se rechazó ese resultado y se injertaron solo los 55 nombres nuevos por voto mayoritario a nivel de nodo, dejando las 714 comunidades de conocimiento intactas (verificado: 0 diferencias de id/comunidad/nombre). Impacto: nueva carpeta `grafo/` con `README.md`, `graph_subproyecto_final.json`, `vault/`, `scripts/`; INDICE_MAESTRO/CHANGELOG actualizados. Fecha: 2026-07-25. |
| SD-28 | **Ejecutar `PDR-01`, la primera pasada completa de correcciones sobre la fase 2.** Detonantes: los 4 puntos de la retroalimentación docente y los 15 hallazgos de desambiguación del certificado de robustez. **Alcance:** MD-01 v1.2→v1.4, DCU-01 v1.0→v2.1 (10→14 casos de uso), las especificaciones 10→14 (de 18 errores de validador a 0), la robustez 10→14 diagramas, y la propagación aguas arriba a MV-01, REQ-01, TRZ-01, PER-01, PRIV-01 y DIS-00. **Decisiones nuevas que introduce:** (a) el `Consentimiento` se separa en **capa base** y **capa de personalización** —lo que hace que revocar la personalización no sea punitivo—; (b) el límite por mensaje sube de 1.500 a **2.500 caracteres**; (c) «acompañante» se declara **alias de producto en uso activo** de `Personaje` en MV-01 §11. **Reversiones declaradas:** `Visitante` pasa a ser clase de dominio, contra el criterio de MV-01 §3/§14 que lo excluía; y `EventoOperativo` deja de ser «vista derivada, no clase» en MV-01 §13.2, a favor de PER-01. **Descartado por no pasar el test de sustitución:** la herencia literal `Usuario <|-- Administrador` que sugería la retroalimentación; se resuelve con el supertipo `TitularDeCuenta`, que conserva la intención sin romper el canon de privacidad. Registro completo en `PDR-01_primera_pasada_correcciones.md`; evidencia por punto del profesor en `RET-01_retroalimentacion_docente.md`. | confirmada (usuario) | Decisión del líder tras la retroalimentación docente (2026-07-31). Impacto: MD-01, DCU-01, las 14 ECU, los 14 DR, MV-01, REQ-01, TRZ-01, PER-01, PRIV-01 y DIS-00. |
| SD-29 | **Revertir el stack a una arquitectura sin servidor.** Interfaz **React 19 + Vite + TypeScript** con **Tailwind CSS v4 + shadcn/ui sobre Base UI**, desplegada en **Vercel**; backend en **AWS Lambda (Node 22 / TypeScript) tras API Gateway**; **DynamoDB** como base operativa; **S3 versionado** para configuración, activos y respaldos; **autenticación propia** (Argon2id + sesión en cookie firmada), descartando Cognito por incompatibilidad con RF-20, que prohíbe recolectar correo o teléfono. **Groq `gpt-oss-20b` se mantiene** (se evaluó Bedrock y se descartó: sin capa gratuita equivalente y con retrabajo en ECU-06/DR-06/DCU-01). **Ninguna condición de reversa declarada en ADR-001 disparó el cambio** — D2 preveía Postgres, D5 preveía Render: es decisión nueva y exógena, y así queda declarada. **Frontera:** decide motores y topología, **no esquemas**; el diseño de claves, la tabla de *endpoints* y el inventario físico de S3 siguen diferidos a `ARQ-01`, **posterior al diagrama de clases y su CDR**. **Declarado como no-objetivo:** cachear respuestas del LLM, que sería persistir el chat (PRIV-R2/RNF-03/RF-13). Registro completo en `ADR-002_reversion_stack_serverless.md`. | confirmada (usuario) | Decisión del equipo al planificar la construcción. Impacto: ADR-001 (D1/D2/D5 superadas), MV-01 v2.6, REQ-01 v1.5, PER-01 v1.2, PRIV-01 v1.5, SEG-01 v1.2, TRZ-01 v1.6, PLAN-01 v1.1, DIS-00 v1.1, DIS-01 v1.1, MD-01 v1.5 y DCU-01 v2.2 (solo anexos; los `.puml` intactos). Artefactos nuevos: `ADR-002`, `HECHOS_CANONICOS.md` y `scripts/verificar_coherencia.py`. |
| SD-30 | **Producir los diagramas de secuencia `DS-01…DS-14`** (paso 6 de ICONIX) con la skill `uml-sequence-diagram` en modo *Generar*, tras el piloto `DS-06`/`DS-07` como rebanada vertical completa. **Decisiones que la gobiernan:** (a) la **capa de infraestructura NO se ejecuta** y se declara como no ejecutada (`E-1` de `DS-00`) — el diseño físico va a `ARQ-01`, después del diagrama de clases y su CDR (`ADR-002 §1`); los participantes se derivan **solo** de los `DR-XX`, sin `INF_`. (b) **No se crea `ARQ-01`**; los supuestos usados se declaran en `DS-00`. (c) SVG con **generador propio** (`generar_svg_secuencia.py`) que hereda el sistema visual de robustez y **corrige la causa raíz** del defecto de legibilidad: el generador anterior enruta N arcos por canales de capacidad fija con aritmética modular —`CANAL_CC` tiene 28 px y 4 posiciones para los 23 arcos control-control de `DR-06`, así que a partir del quinto **recicla una coordenada ya usada**— y su anti-solape compara etiquetas solo contra otras etiquetas, **nunca contra las líneas**. El nuevo traza el arco 10 px por debajo del texto (garantía geométrica, no búsqueda de hueco) y **aborta** si un pase de verificación post-layout detecta colisión. **Dos correcciones aguas arriba que el paso destapó:** `H-8` — `FE-02` de `CU-01` tenía dos desenlaces incompatibles (`ECU-01` decía «vuelve a P-01» tres veces, incluida `CA-08`; `DR-01` dirigía a P-02 y terminaba), resuelto **a favor de `ECU-01`**; y `H-9` — `DR-04` había omitido `EventoOperativo`, que `ECU-04 §7` declara como concepto que **permanece** fuera de la cascada «y por eso debe ser irreidentificable» (`RE-06`), lo que dejaba `CA-11` sin nada de donde derivar prueba. **(d) Los siete hallazgos restantes se aplican en la misma decisión**, tras una revisión de consistencia del plan. El grande es **`H-1`, que resultó ser dos defectos con una raíz**: el `EventoOperativo` se registraba **al cerrar** la conversación (`H-1a`) y las «llamadas al chat de 7 días» se contaban desde **`Conversacion`** (`H-1b`). Ambos son insostenibles por la misma razón — **la `Conversacion` no se persiste** (`RF-13`, `PRIV-01 §2`: «No (nunca)»), y `MD-01 §3` creó `EventoOperativo` precisamente por eso: «sin esta clase la tasa de 7 días de `ECU-09` sería incomputable». El evento pasa a escribirse **por llamada**, y las dos cifras de ventana pasan a salir de él. **Decisión que `H-1a` obligó a tomar y que el plan no había previsto:** se registra **una vez por llamada efectivamente hecha al proveedor** — `FE-06`/`FE-07` (llamada hecha que falla) **sí** cuentan, porque `MET-07` mide «peticiones OK + *fallback* / **totales**» y sin ellas el denominador solo contendría éxitos; `FE-04` (*kill switch*) y `FE-05` (límite de tasa) **no**, porque cortan antes de tocar al proveedor. | confirmada (usuario) | **Resultado:** 14 diagramas en **0 errores**, **150/150** controladores cubiertos, 0 flujos sin fragmento, **282 mensajes** (`H-22`), **192 operaciones** (`H-23`) sobre las **16 clases** de `MD-01` y **3** del espacio de la solución (2 %), **181 casos de prueba** (`H-24`) y 14 SVG sin colisiones. **Impacto:** nueva carpeta `docs/07_casos_uso/secuencia/`; `DR-01` y `DR-04` a **v2.1**, `DR-06` y `DR-09` a **v2.1**, `DR-00` a **v2.2**; `ECU-06` y `ECU-09` a **v2.1**, `ECU-05`/`ECU-07`/`ECU-13` retocados; `PER-01` a **v1.3**; `MD-01` a **v1.6** (solo anexo, el `.puml` intacto); `HECHOS_CANONICOS` a v1.2. **Trayectoria de `H-20`, que se movió dos veces:** 261 → **263** (por `H-9`, que añadió `EventoOperativo` a `DR-04`) → **262** (por `H-1b`, que dejó `Conversacion` huérfana en `DR-09`). `H-21` se quedó en **150** todo el tiempo. También: `generar_svg_robustez.py` con sus conteos actualizados y los SVG regenerados; `ESTADO_PIPELINE`, `INDICE_MAESTRO`, `TRZ-01`, `CHANGELOG`, `README`. **Excepción declarada `E-2` de `DS-00`:** seis diagramas de pantalla única quedan entre 62 % y 78 % de control centralizado; se intentó repartir en los seis y se aplicó lo que mejoraba el diseño, pero lo que queda es irreducible — el mismo criterio que `DR-00 §5 E-1`. **Sigue abierto:** `PER-H5` (los respaldos en S3 escapan al borrado en cascada), que se cierra en `ARQ-01`. |
| SD-31 | **Corregir la causa raiz del defecto de legibilidad de los SVG de robustez y consolidar los pendientes.** El generador propio se llevaba tres defectos encadenados: (a) repartia los arcos con `idx % N` sobre bandas de **capacidad fija** —`CANAL_CC` daba 4 posiciones y `DR-06` pide 24, asi que del quinto en adelante reciclaba una coordenada—; (b) su anti-solape comparaba cada etiqueta **solo contra otras etiquetas, nunca contra las lineas**, con lo que por construccion no podia ver el defecto que el ojo ve primero, y si no hallaba hueco colocaba igual sin avisar; y (c) **nada verificaba el resultado**, que es lo que dejo pasar a los dos anteriores. Al medir la demanda real se descubrio que **no era `DR-06`: los catorce diagramas desbordaban algun canal**. Se sustituye el modulo por **coloreado de intervalos** (dos arcos comparten coordenada si sus tramos verticales no se solapan: `DR-06` baja de 24 arcos a 12 pistas), se parte cada hueco entre carriles en **dos bandas** —una de etiquetas sin una sola pista y otra de pistas, de modo que un chip no puede taparle la linea a nadie—, la **altura de cada caja pasa a depender de cuantos arcos salen de ella** (con catorce arcos en una caja de 37px los anclajes salian a 2,5px y catorce etiquetas de 14px no podian no solaparse), y se añade un **pase geometrico que aborta**: el lote se escribe entero o no se escribe. En la misma decision se crea la seccion **«Pendientes declarados»** de `ESTADO_PIPELINE`, que reune los **once** que estaban dispersos —`PER-H5` se mencionaba en 18 archivos y `COD-01` en 6, sin ningun sitio donde estuvieran juntos—. | confirmada (usuario) | **Resultado:** los **14 SVG regenerados en cero colisiones**, con los `.puml` **intactos** y los conteos sin moverse (262 elementos, 15/38/150/59). El lienzo pasa de 1.000 a **1.340 px** y los diagramas crecen en alto —`DR-06` llega a 2.488 px—, decision del usuario: *«que quede en un mismo diagrama asi sea gigante»*. **Correccion de una nota propia:** `SD-30` habia recomendado regenerar `MD-01.svg` y `DCU-01.svg` «con la reticula corregida»; es falso, **los produce PlantUML** y sus `.puml` no cambian. Corregido en `DS-00 §10`/`§11` y `ESTADO_PIPELINE`. **Alcance:** solo pintura y prosa —ningun `.puml` tocado, ningun hecho canonico movido. |
| SD-32 | **Producir el modelo de clases de diseño** (`MC-00`, `MC-01`, matriz de procedencia, `COD-01`), séptimo y último artefacto ICONIX antes del CDR, con la skill `uml-design-class-model`. **37 clases** —las 16 de `MD-01` con nombre idéntico más **21 del espacio de la solución**—, **200 operaciones** = 192 nombres distintos + 8 repeticiones, **35 atributos**, **11 enumerados** y **73 relaciones**. Cierra el pendiente #2 de `ESTADO_PIPELINE` (`COD-01`), declarado allí como «lo cierra: el diagrama de clases de diseño». **Dos capas declaradas NO ejecutadas:** la de infraestructura, heredando `E-1` de `DS-00` (el diseño físico es `ARQ-01`, tras el CDR), y el **render del `.svg`**, que no se pudo generar por no haber PlantUML en el entorno — se declara sin afirmar que se revisó. **Doce hallazgos aplicados sobre los insumos**, uno evaluado y revertido con medición (`H-C`) y uno declarado sin arreglo limpio (`H-N`). El más relevante, `H-D`, resultó tener su **raíz en `DR-11` y `DR-13`**, no en secuencia: los `DS` copiaban fielmente su robustez, que es lo que el método exige. | confirmada (usuario) | Decisión del líder tras revisar los hallazgos agrupados por destinatario. **Verificación de no regresión:** línea base tomada antes de empezar y reproducida al cerrar — los cinco validadores en 0 errores, los 14 de secuencia con las **mismas 6** advertencias, `verificar_coherencia.py` en verde, y los SVG de robustez y secuencia regenerados con 0 colisiones conservando 262 elementos. Impacto: nueva carpeta `docs/07_casos_uso/clases/`; `MV-01` v2.7, `MD-01` v1.7 (solo `.md`), `DOP-01` v1.2, `DS-00` v1.4, `TRZ-DS-01` v1.1, `DR-11`/`DR-13` en su etiqueta de frontera. **Dos defectos del validador de la skill** (`H-M`, `H-M2`) se reportan a su mantenedor: son ajenos a este repositorio. |
| SD-33 | **Cerrar `PER-H5`: el MVP no respalda el almacén de datos personales** (`ADR-003`). El almacén operativo —las 7 entidades— deja de tener respaldos, exportaciones y recuperación a un punto en el tiempo; el **versionado se queda solo donde no hay dato personal**, que es la configuración y los activos, que era la razón declarada por la que `ADR-002-D6` eligió S3. **Se cierra quitando el segundo almacén, no acotándolo:** sin respaldo no hay copia que escape a la cascada de `PER-T1`. **Reversión declarada:** el plan de Codex sí pedía respaldo —en su DoD de release, su tabla de riesgos y `backup_sqlite.py`—, pero atado a **SQLite**, cuyo modo de fallo (perder un archivo único en un alojamiento gratuito) desapareció con `ADR-002-D5`. Se conserva su **intención** separando «servicio recuperable» —que lo sigue siendo, porque la configuración y los textos legales siguen versionados— de «dato personal recuperable tras su borrado», que es lo que rompía el canon. **Descartadas y preparadas:** respaldos acotados con expiración, y cifrado por usuario (*crypto-shredding*), ambas en `ADR-003 §3` por si se dispara la condición de reversa. | confirmada (usuario) | Decisión del líder tras el análisis de opciones. **Se pudo tomar antes de `ARQ-01` sin romper `CLAUDE.md §6`** porque no es diseño físico sino un **no-objetivo declarado**, de la misma clase que «no cachear respuestas del LLM». **Verificado por lectura directa:** ningún requisito de `REQ-01` ni de `VIS-01` promete respaldo, durabilidad ni recuperación. **Lo que NO cierra, y se dice:** `RF-24` pasa de **dos excepciones abiertas a una** — `PER-H2` (la ventana de «+30 días») y `V6-b` (la frontera legal) siguen abiertos. **Precio declarado:** la pérdida del almacén operativo es irrecuperable; `R-11` se reformula de «alto (canon/legal)» a «medio (operativo)». Impacto: nuevo `ADR-003`; `ADR-002-D6` acotada; `PER-01` v1.4, `PRIV-01` v1.6, `REQ-01` v1.6, `PLAN-01` v1.2; `MC-01`, `MC-00`, `COD-01` y `TRZ-DS-01` actualizados. |
| SD-34 | **Marcar el respaldo de la base de datos como obsoleto y cerrar el registro de `RF-24`.** El equipo confirma que **no se implementará ningún respaldo**, por **velocidad de entrega**. `ADR-003` pasa de superar el ítem de respaldo del DoD del plan de Codex «solo en lo que toca al dato personal» a superarlo **por completo**, y el valor entra en la tabla de **valores obsoletos** de `HECHOS_CANONICOS`, que es el mecanismo que el proyecto ya tiene para esto. **No se edita `00_PLAN_CODEX_ORIGINAL.md`**, fuente primaria verbatim. **Segunda mitad, y es corrección de un defecto propio:** la propagación de `SD-33` había quedado **incompleta** — cinco sitios seguían afirmando que `RF-24` falla por el respaldo (`TRZ-01`, `CP-00`, `CP-04`, la nota de `DS-04.puml` y, el más serio, el **criterio de aceptación** de `ECU-04`, que mantenía el acotador «desde la aplicación» en cuatro lugares). Sin respaldo, ese acotador sugería que existe un sitio fuera de la aplicación donde el dato sobrevive; ya no existe, y se retira igual que se retiró de `PRIV-R11`. `RA-01` de `ECU-04` también razonaba sobre «retención de respaldo por entorno», que ya no aplica: los «+30 días» son retención **del propio registro de cuenta**. | confirmada (usuario) | Decisión del líder por velocidad de entrega. **Alcance acotado a `RF-24` a petición suya:** quedan **decididas pero sin escribir** tres cosas de la sesión anterior — el riesgo residual de `RF-18`, el valor inicial del *kill switch* (habilitado) y la corrección del sobre-claim de `ADR-003 §4`. **Verificación de no regresión:** línea base tomada antes de empezar y reproducida al cerrar. Impacto: `HECHOS_CANONICOS` v1.5, `ADR-003` v1.1, `ECU-04` v2.2, `TRZ-01` v1.8, `CP-00`/`CP-04`, `DS-04.puml` (solo la nota) con su SVG regenerado. **`PER-H2` y `V6-b` siguen abiertos**: `RF-24` no se cumple de forma inmediata, y eso no cambia aquí. |
| SD-35 | **Cerrar `PER-H2` y el estado inicial del *kill switch*** (`ADR-004`). **D1:** la supresión de cuenta es **física e inmediata** — sin ventana de gracia, sin marca de baja, sin borrado lógico. **`PER-H2` resultó ser una ambigüedad sintáctica de la fuente, no un hueco de diseño:** «Hasta eliminación o cierre + 30 días» (plan §4.14) admite dos análisis, y el hallazgo asumía —sin declararlo— el que pone al plan en contradicción con `RF-24`. **El MVP no tiene «cierre de cuenta»**, verificado sobre los 26 RF de `REQ-01`: hay iniciar y cerrar **sesión**, reiniciar, revocar y eliminar, pero ninguna baja ni desactivación, y `VIS-01 §5` excluye la suspensión individual. Esa ventana se refería a un escenario que aquí no existe. **Marcado `[I2]`: es interpretación de la fuente primaria, no evidencia**, y así se escribe. **D2:** el *kill switch* arranca **habilitado**, cerrando `PRE-03` de `ECU-10` —abierto desde antes y vuelto urgente por `ADR-003`, porque sin respaldo un almacén perdido arranca en un estado que nadie había decidido—. **No es *fail-closed*, y se declara:** el motivo es la conveniencia de la demostración académica, y la condición de reversa es material — **en cuanto haya personas reales, pasa a `deshabilitado`**. | confirmada (usuario) | Decisión del líder. **Efecto principal: `RF-24` pasa a cumplirse** y deja de ser el único requisito declarado como incumplido del proyecto; sus dos excepciones quedan cerradas (`PER-H5` en SD-33, `PER-H2` aquí). **Con la precisión que evita el sobre-claim:** se cumple **según el diseño**; la inmediatez solo se verifica contra una implementación, y eso es fase 4. **Tres hallazgos cierran a la vez** —`PER-H2`, `RA-01` de `ECU-04` y `RA-03` de `ECU-11`— porque eran la misma pregunta registrada en tres sitios. **Se pudo decidir antes de `ARQ-01`** por el mismo test que dejó pasar `ADR-003`: dice qué hace el sistema, no claves ni índices; `ARQ-01` hereda una **restricción** (borrado físico, sin `deletedAt`), no una pregunta. **Y no necesitó `V6-b`**: el borrado inmediato es la opción más conservadora bajo la Ley 1581, así que reduce su superficie en vez de depender de ella. **`V6-b` y `PER-H4` siguen abiertos.** En la misma pasada se corrigió un sobre-claim de `ADR-003 §4` —«el dato es recreable por su propio titular» es cierto de las cuentas y **falso** de la auditoría y la telemetría— y se declaró el **riesgo residual de `RF-18`**: sin respaldo, la traza de auditoría del *kill switch* no sobrevive a la pérdida del almacén. |
| SD-36 | **Cerrar la propagación que `SD-35` dejó a medias.** Al cerrar `PER-H2` con `ADR-004-D1`, cuatro afirmaciones vigentes se quedaron diciendo lo contrario que los artefactos: (a) **`CLAUDE.md` y `AGENTS.md`** —el canon que lee cualquier agente al empezar— seguían declarando `PER-H2` como «el que más pesa» y afirmando que impide cumplir `RF-24`; (b) la nota de `ESTADO_PIPELINE` «Actualizado en SD-33» heredó esa marca a `PER-H2` sin que `SD-35` la retirara; (c) la fila de `RF-24` en `TRZ-01 §2` conservaba el símbolo de incumplimiento **tres líneas por encima** de su propia nota que dice que ya se cumple; y (d) el `README` hablaba de «once pendientes» cuando la tabla tiene doce filas con tres cerradas: son **nueve** abiertos. Se corrige además un defecto ajeno a `SD-35`: `E-3` de `DS-00` declaraba **diez** mensajes dirigidos a un actor y son **once** —contados por diagrama—, y `E-3` es una excepción cuyo valor entero es que la sostiene una medición. | confirmada (usuario) | **Hallado en una revisión cruzada tras el *pull* de `SD-33`/`SD-34`/`SD-35`.** Ninguno es de fondo: el trabajo de esas tres decisiones es sólido y sus validadores pasan. Son de **propagación**, la misma clase de defecto que `HECHOS_CANONICOS` existe para detectar — y el que más pesaba dejaba el canon contradiciendo a los artefactos. **Estado tras la corrección:** quedan **nueve** pendientes abiertos y **ninguno rompe un requisito vigente**; `RF-24` se cumple según el diseño, con la verificación contra implementación diferida a fase 4 (`TRZ-01 §2`). **Verificado:** los cuatro bloques de `verificar_coherencia.py`, los tres validadores de skill sobre los 14+14+15 artefactos, `MC-01` en 0 errores con el validador corregido, y los dos generadores de SVG sin colisiones. |
| SD-37 | **Ejecutar el CDR, la compuerta entre el diseño detallado y el código** (`CDR-01`, skill `iconix-cdr-review`, modo completo). Hito 3 de la Fase 2. **Nueve de las diez reglas ejecutadas**; la **#2** (generar e inspeccionar las cabeceras de código) queda **diferida con disparador** —se ejerce tras aplicar las correcciones, y las cabeceras pasan revisión humana antes de versionarse—, que es ausencia razonada y no silenciosa. **La regla #4 la ejerció el líder por el equipo**, y su respuesta es sustantiva, no un trámite. **13 hallazgos, dos de ellos Mayores.** **`H-01`:** doce fragmentos `break` sobre flujos cuyo texto declara «Vuelve al paso N» — `break` abandona el fragmento envolvente en UML 2.5, así que el diseño construye un callejón sin salida donde la especificación pide reintento; el más grave es el *timeout* de 20 s de `DS-06:105`. Es sistemático y su causa está identificada: **el operador se eligió por categoría (`FE`→`break`), no por desenlace**, y lo prueba que los 15 `break` sobre flujos que sí terminan son correctos. Se corrige sustituyendo por `opt`. **`H-02`:** la atomicidad que `ECU-04 RE-04` promete («todo o nada») se realiza con **una auto-llamada, `deshacerLoSuprimido()`**, sin transacción, compensación ni bitácora — «magia» en el sentido de Rosenberg. Colisiona a tres bandas con `ADR-002-D5` (DynamoDB no revierte escrituras confirmadas fuera de `TransactWriteItems`), con la cascada repartida en cuatro mensajes a cuatro entidades, y con `ADR-003` (sin respaldo del almacén): una vez borrada la `CapsulaDePerfil` no hay de dónde recuperarla. **Decisión sobre `H-02`:** se **reabre `ECU-04 RE-04`** y la promesa pasa de «atómico todo-o-nada» a **borrado ordenado, tolerante a fallo parcial y reintentable** —consentimiento primero, titular al final—, que entrega la garantía que le importa al usuario sin transacciones y **sin adelantar `ARQ-01`**. Se descartó `TransactWriteItems` por comprar una garantía más cara de la que el MVP necesita, obligar a fijar claves antes de tiempo (`ADR-002 §1`) y chocar con su tope de 100 ítems frente a un `ContadorDeUsoDiario` **por día de uso**. **`H-03`** (la pantalla P-13 con 17 operaciones en tres familias paralelas del mismo comportamiento) se **difiere con riesgo aceptado**: funciona, y partirla alcanza tres casos de uso con sus secuencias y sus pruebas. Los otros nueve se corrigen. **Veredicto propuesto: `Reinspección requerida`** —lo determina el líder—, con **alcance acotado** al modo *Verificar retrabajo* sobre los artefactos tocados; no procede repetir la revisión completa. | confirmada (usuario) | Decisión del líder, con la regla #4 ejercida por el equipo tras pedir y recibir la explicación técnica de `H-02`. **Lo que hizo creíble la revisión, y conviene no perder:** el validador se **calibró contra los 16 fixtures** de la skill antes de usarlo —`bueno` y `falsos_positivos` en cero señales, los 14 de defecto disparando—, porque `MC-00 §5.2` ya había registrado que esa misma herramienta emitió `LISTO PARA EL CDR` con **0 operaciones parseadas**. Y el marco de la primera corrida estaba mal: **55 advertencias bajaron a 13** al pasarle el plan de pruebas completo en vez del solo índice `CP-00` —42 eran artefactos de invocación, y llevarlos al acta la habría envenenado—. **Ningún artefacto se tocó para conseguirlo.** Se reprodujeron los **8 conteos canónicos** contra los `.puml` (150/282/181/37/11/200/16/14, todos exactos) y `verificar_procedencia_mc01.py` en 0 discrepancias. El fan-out de la regla #10 se auditó con **replay del 100 %**: 72/72 filas verificadas. **Dos errores de instrumentación propios quedan declarados en el acta** —el extractor de relaciones perdía las 9 con multiplicidad entrecomillada, y el normalizador del replay convertía `**tono**,` en `tono ,`—, porque una revisión que no publica sus propios fallos no merece crédito por los ajenos. **Tasa de hallazgos: 0,37 por página** contra la base medida de 0,4: dentro de lo esperado, así que no aplica la advertencia de revisión superficial. Impacto: nuevo `CDR-01`; `ESTADO_PIPELINE`, `CAPSULA_CONTEXTO`, `INDICE_MAESTRO` y `CHANGELOG` actualizados. **`HECHOS_CANONICOS` no se toca**: `H-13` (las «73 relaciones» incluyen 2 conectores entre notas; las reales son 71) es un hallazgo enrutado, y mover una cifra es una decisión, no una edición. |
| SD-38 | **Los commits de este repositorio los firma el equipo humano y nadie mas.** No se admite ningun trailer de coautoria de herramientas, asistentes ni modelos. Se retiraron los que existian en la historia y se impide mecanicamente que vuelvan a entrar. | confirmada (usuario) | **Reescritura:** `git filter-branch --msg-filter` sobre las 4 ramas desde la ultima base limpia (`b8c551b`). **Cero perdida, verificada contra un respaldo en *bundle*:** los **163** pares de commits comparados tienen el **SHA de arbol identico** —o sea, el contenido no cambia ni un byte—, y autor, correo, *committer* y **ambas fechas** quedan intactos; la unica diferencia admitida en cada mensaje es la linea retirada. Los 19 commits anteriores a la base conservan su SHA. **Verificado con tres barridos** sobre los **5.413 objetos** de la base, sobre los archivos de `.git/` fuera de `objects/` y sobre `git log --all`: cero coincidencias. Se borro `refs/original`, se expiraron los reflogs y se purgo con `gc --prune=now`. **Limite declarado:** tras un `push --force` los objetos siguen en el servidor de GitHub hasta que pase su recolector, que no se dispara desde fuera; el repositorio no tiene *forks* ni *pull requests*, asi que quedan inalcanzables desde cualquier referencia. **Prevencion en tres capas:** el *hook* versionado `.githooks/commit-msg` retira la linea **antes de que el commit exista** —de cualquier herramienta, conservando las lineas en blanco entre parrafos—; la regla queda escrita en `CLAUDE.md §2` y `AGENTS.md §3`; y esta entrada explica el motivo, para que nadie desmonte el *hook* sin saber por que estaba. | **Renumerada el 2026-08-04, durante `SD-37`:** esta decision se registro como `SD-32`, identificador que ocupaban **dos** decisiones distintas —esta y la del modelo de clases de diseno—, de modo que media docena de archivos citaban `SD-32` para referirse a cosas diferentes. Se renumero **esta** y no la otra porque sus citas son cuatro y viven en archivos vivos (`CLAUDE.md §2`, `AGENTS.md §3` y dos lineas del `CHANGELOG`), mientras que las ~25 de la otra estan en buena parte dentro de **changelogs historicos de artefactos** (`MD-01 v1.7`, `MV-01 v2.7`, `DOP-01 v1.2`, `DS-00 v1.4`, `TRZ-DS-01 v1.1`, `HECHOS_CANONICOS v1.4`), que registran lo decidido entonces bajo ese ID: reescribirlos seria falsear la historia, que es lo que `SD-31` ya rechazo por escrito. **Consecuencia declarada:** una decision del **2026-08-02** queda numerada tras las del 08-04. El ID es un identificador, no una marca de tiempo, y la fecha real vive en el `CHANGELOG` (`v0.17.3`).
| SD-39 | **Ejecutar el retrabajo de los 20 hallazgos del `CDR-01`**, artefacto por artefacto y con la skill dueña de cada uno. **Las cuatro decisiones del líder que lo abrieron:** (a) **`H-14`** — se **retira** la obligación de auditoría de `ECU-11 RE-06`: ningún `RF` la respalda, `RF-22` solo pide que la cápsula deje de existir y `RF-18` —el único de auditoría— es del *kill switch* y dice «sin datos de usuario»; auditar la acción de un usuario sobre sus propios datos persistiría un registro sobre esa persona, contra minimización y contra el canon no punitivo. (b) **`H-15`** y **`H-16`** reciben el **mismo tratamiento que `H-02`**: se cambia la promesa, no se inventa mecanismo — `ECU-11 FE-04` pasa de «deshacer el borrado incompleto» a informar el fallo sin haber suprimido nada, porque sobre **una** entidad no existe el estado intermedio que la versión anterior protegía (contraste con `H-02`, donde la atomicidad era **imposible**; aquí era **innecesaria**); y `ECU-05 CA-10` deja de prometer que «nada de lo no confirmado quedó escrito», que es falso porque `DS-05` crea el `Consentimiento` en el paso 5 y la cápsula en el 8. (c) **`H-17`** — la obligación de `PRIV-R10` en `ECU-08` se **reformula** a «no expone dato sensible ni contenido individual»: la anterior era incumplible, porque cada fila del directorio **es** un titular. (d) **Cierre:** reinspección, `MC-01.svg`, cabeceras, esta entrada y la propagación. **Cifras que el retrabajo movió, y por eso esto es decisión y no edición:** relaciones **73 → 71** (`H-13`, dos conectores entre notas salen del conteo) **→ 80** (las 9 dependencias, ver abajo); `H-22` 282 → **283**; `H-23` 192 → **193**; `H-25` 37 → **43**; `H-26` 200 → **201**; `H-27` 35 → **51**. **Cuatro cosas más que se decidieron durante la ejecución, cada una con su aprobación:** (1) **`H-04` se resolvió con seis de los siete tipos** — `Sesion` queda **sin declarar a propósito**, porque sus campos *son* el mecanismo de sesión que `E-1` y `ADR-002 §1` difieren a `ARQ-01`; declararla habría revertido una exclusión que el propio CDR validó. (2) **Las 9 dependencias hacia los tipos de transferencia se dibujan**, y el argumento no es la legibilidad: `MC-01_matriz_procedencia §1` fija que las relaciones de solución van «solo donde una operación navega», y una operación que **devuelve** un tipo navega hacia él — **el modelo incumplía su propia regla**. Son 9 y no 6 porque seis son los tipos y nueve las clases que los devuelven. (3) **Título en los tres diagramas que renderiza PlantUML** (`MC-01`, `MD-01`, `DCU-01`), no solo en uno: era el defecto que `PDR-01 §7` cazó en nueve de catorce, y arreglarlo en uno habría cambiado un defecto compartido por una inconsistencia. Toca `MD-01`, de `/uml-domain-modeler`, y se declara. (4) **Las cabeceras de la regla #2 se versionan** (`MC-01_cabeceras.txt`) **con red**: `verificar_coherencia.py` gana un **bloque 5** que falla si un derivado queda más antiguo que su `.puml`. | confirmada (usuario) | **Alcance de esta entrada: la contabilidad, no el veredicto.** El veredicto de `CDR-01 v1.2` y la verificación del retrabajo se hacen **en sesión aparte**, por decisión del líder, para que quien verifique no arrastre el anclaje de quien aplicó. Sigue sin satisfacer `IEEE 1028 §6.5.6.5` —*«rework verified by other than the author»*— y así se declara: mitiga el sesgo de recuerdo, no lo sustituye por independencia real. **Se tocó el instrumento de verificación, y eso no es una edición documental:** `scripts/verificar_coherencia.py` gana el bloque 5, que compara **fechas y no contenidos** —renombrar un alias no cambia el `.svg` ni un byte, y comparar contenidos daría falso aviso justo ahí, como pasó con tres `DR` en `H-20`— y **solo mira cambios sin comitear**, igual que el bloque 3, porque git no conserva fechas. **Probado haciéndolo fallar**, no solo pasar. **Ocho hallazgos que el propio retrabajo destapó y el acta no tenía:** `MC-00 §6` no daba estado a `H-E`/`H-I`/`H-O` —los tres que siguen abiertos—; la matriz remitía «los seis casos sin procedencia» a `§6` cuando son **siete** y están en `§9`; daba «37 dependencias del espacio de la solución» cuando era el número de **clases** (son **42 + 12 = 54**, e idéntico antes y después de `H-04`, medido contra `HEAD`); `H-20` alcanzaba **seis** archivos y no tres —el alias minoritario de P-04 había llegado a `MC-01` y `COD-01`—; `DOP-01 §2` nombraba una receptora con el alias de `DR-06` en vez del de `DS-06:46`; `COD-01` no tenía fila `v1.2` y el historial de `DOP-01` tenía `v0.1` fuera de orden; `INDICE_MAESTRO` declaraba `TRZ-DS-01` en `v1.1` y `v1.0` cuando va por `v1.3`, **y el bloque 4 no puede verlo** porque su regex lee `TRZ-DS-01` como `DS-01`, único ID compuesto del repositorio (dato corregido, **script no**); y la **regla #2 destapó que 20 de las 43 clases se emitían con nombre que no es identificador válido**, defecto del generador de la skill —usa la etiqueta y no el alias, y las 20 tienen alias válido—, resuelto con herramienta **propia y versionada** (`scripts/generar_cabeceras_mc01.py`) en vez de un parche local invisible para el equipo. **Verificación de no regresión, reproducida al cerrar:** 14 `DR` en 0 errores y **262 elementos** (15/38/150/59); 14 `DS` en 0 errores y **6 advertencias** (`E-2`), con la capa «cierre de participantes contra `DR-XX`» ejecutada 14/14; `MC-01` en 0 errores y **9 advertencias**; procedencia **sin discrepancias**; 31 pares `.puml`/`.svg` frescos; los cinco bloques de `verificar_coherencia.py` en 0. |

| SD-40 | **Verificar el retrabajo del `CDR-01` con un modelo distinto, y cerrar la propagación del canon que ese barrido destapó.** **D1 — quién verifica.** `SD-39` previó una «sesión aparte» del **mismo** modelo, que solo mitiga el sesgo de recuerdo. Se ejecutó con **OpenAI Codex, modelo `gpt-5.6-sol`**, sobre el retrabajo aplicado por **Claude Opus**, con encuadre **adversarial** —el encargo fue *refutar* cada corrección, no confirmarla— y alcance de escritura acotado a **un solo archivo**, el acta. Resultado: **`CDR-01 v1.4`**, con 22 de los 28 objetos confirmados y **ocho hallazgos nuevos** (`VI-01`…`VI-08`). **D2 — el veredicto no se toca aquí.** Sigue en **`Reinspección requerida`**, ahora sostenido por tres Mayores propios en vez de por la falta de verificación; **lo determina el líder** (IEEE 1028 §5.2.1). **D3 — `VI-06` se cierra por propagación, no por rework.** Las cifras las decidió `SD-39`; llevarlas a sus consumidores es **edición, no decisión**, por la regla de `HECHOS_CANONICOS`. **D4 — `VI-01`, `VI-02` y `VI-03` NO se corrigen aquí**, y el motivo no es de agenda: quien escribe esta entrada es **Claude Opus, el mismo modelo que aplicó `SD-39`**; corregir los Mayores que un revisor independiente acaba de levantar contra su propio trabajo reconstruye exactamente el problema que la verificación existía para evitar. Van a sus skills dueñas, y después del veredicto del líder. | confirmada (usuario) | **Lo que la independencia real compró, y es el argumento de fondo para repetirla:** `H-01` y `H-02` **no pasaron**. La sustitución de 17 `break` por `opt` es exacta en recuento —33 correctos, 17 sustituidos— pero **`opt` no vuelve al paso que el texto pide: solo salta su cuerpo y continúa**. En `DS-04` el usuario cancela la eliminación y el flujo **sigue hasta la cascada de borrado**, contra `ECU-04 FA-03` («El Sistema no suprime nada»); en `DS-06` un *timeout* del proveedor continúa hacia «texto generado». El diagnóstico de `SD-39` era correcto y el remedio cambió **un callejón sin salida por una caída al éxito** — un defecto que ninguna sesión del autor iba a ver sobre sí misma. `H-14`, en cambio, quedó **confirmado**. **El barrido de propagación encontró más de lo que el acta pedía.** `VI-06` listaba ocho ubicaciones; el contraste de las **siete** cifras que `SD-39` movió contra todos los `.md` dio **once puntos vivos**, y los cuatro que el acta no vio son los peores por su naturaleza: **`DS-00` se contradecía a dos líneas** —tabla en 282, nota en 283—, con el desajuste localizado en la fila de `DS-10` (20 declarados, 21 reales: el mensaje que añadió `H-18`); **`TRZ-DS-01` declaraba 282 y 192 bajo el rótulo «los cuatro totales son hechos canónicos»**, que es la forma más eficaz de propagar un error, porque quien lo lee deja de comprobarlo; **`DOP-01`, dueño de `H-23`, decía 192** trece líneas por encima de su propia §8 bis, que decía 193; y **`COD-01` sumaba 37** mientras su ficha decía 43. **Causa raíz, y queda declarada:** `SD-39` movió seis hechos y **no escribió ni una fila de valor obsoleto**, contra la regla de §Mantenimiento de `HECHOS_CANONICOS` — por eso `VALORES_OBSOLETOS` no podía cazar ninguno. Se repara con la fila que faltaba. **Una ambigüedad resuelta en vez de propagada:** `H-28` vale **27** en `MC-01` y **21** en la capa de secuencia, porque los seis tipos de transferencia de `H-04` no aparecen en ningún diagrama de secuencia; propagar 27 a `DOP-01` habría afirmado algo falso, así que se acota el hecho y se documenta la distinción. **Verificación:** los **cinco bloques** de `verificar_coherencia.py` en 0 —el 4 cazó dos desfases reales durante la propia pasada— y `verificar_procedencia_mc01.py` **sin discrepancias** (43 clases, 27 de solución, 201 operaciones), que además cierra una de las brechas dinámicas que Codex declaró por no tener Python. **Y se cerró la otra: se ejercitaron las tres guardas de `generar_cabeceras_mc01.py`** con un generador simulado y un modelo sintético, ambos **fuera del repositorio**, que quedó intacto. **Resultado: dos de tres.** Las guardas 1 —nombre inválido sin alias— y 3 —etiqueta inválida usada fuera de su declaración— **abortan con código 3**, como prometen. **La guarda 2 es inalcanzable** (`SD-40-H1`, pendiente 22): su regex captura el alias con `(\w+)`, que **corta en el primer carácter no-palabra**, así que `as B_Landing-P01` se lee como `B_Landing` y la comprobación `IDENTIFICADOR.match()` recae sobre un alias **ya truncado**, válido por construcción. No solo no dispara: **emite la clase con un identificador distinto del que el modelo declara, y en silencio** — el mismo modo de fallo que la regla #2 del CDR existía para cazar. No se corrige aquí: tocar el instrumento es decisión aparte. **Riesgo hoy: ninguno** — ningún alias de `MC-01` es malformado, comprobado. **Límite declarado sin adorno:** un modelo distinto **no satisface `IEEE 1028 §6.5.6.5`**; reduce puntos ciegos compartidos, no los elimina. La independencia real la da el equipo humano de `CLAUDE.md §2`. |

| SD-41 | **Corregir los dos hallazgos Mayores que la verificación independiente dejó abiertos** (`VI-01` y `VI-02` de `CDR-01 v1.4`), artefacto por artefacto y con la skill dueña de cada uno. **D1 — quién repara.** Lo aplica **Claude Opus**, el mismo modelo que aplicó `SD-39` y contra cuyo trabajo se levantaron estos hallazgos. Se hace así a propósito y con la distinción escrita: `IEEE 1028 §6.5.6.5` exige que el **verificador** no sea el autor; **no prohíbe que el autor repare sus propios defectos**, que es lo normal. *(La primera versión del plan de esta sesión afirmó lo contrario —que el autor no debía reparar— y era un sobre-dicho: se corrige aquí.)* La independencia se preserva en la **re-verificación**, que vuelve a Codex. **D2 — sustitución 1:1 en robustez**, decidida por el líder antes de empezar: cada controlador viejo se reemplaza por uno nuevo con la semántica correcta, de modo que `H-20` (262 elementos) y `H-21` (150 controladores) **no se muevan**. **D3 — el operador se elige por el desenlace que el texto declara, no por la categoría del flujo.** Es la regla que faltaba, y su ausencia produjo el defecto **dos veces**: `H-01` eligió `break` porque el flujo era `FE`, y su corrección en `SD-39` eligió `opt` porque el flujo era alternativo. Ninguna de las dos leyó la fila. | confirmada (usuario) | **`VI-01` no eran diecisiete defectos: eran cuatro.** ~~Al releer las diecisiete filas de `ECU` una por una, **trece decían «Termina»** —no «Vuelve»—, así que su `break` era **correcto** y `SD-39` los había cambiado a `opt` sin necesidad.~~ **⚠ FALSO, refutado en `SD-43` por `SVI-01`:** las diecisiete filas de `ECU` dicen «Vuelve» o «Cancela y vuelve»; **ninguna dice «Termina»**. La relectura que esta entrada presume **no se hizo bien**, y el error se corrige en `SD-43`: las trece pasan a `loop` + `alt`. Los cuatro restantes —`DS-04:41` (`FA-03` «Cancela y vuelve al paso 1»), `DS-04:51`, `DS-06:73` y `DS-06:79`— sí caían al flujo de éxito, y son los que pasan a `alt` con el sufijo en la rama excluyente. El caso que mejor lo ilustra es `DS-04`: el Usuario cancelaba la eliminación y el flujo **continuaba a la cascada de borrado**, contra `ECU-04 FA-03`, que dice «El Sistema no suprime nada». **Coste declarado:** el `alt` empuja tres fragmentos un nivel más abajo en `DS-06` y el anidamiento llega a 3–4 niveles, así que las advertencias del validador de secuencia suben de **6 a 10**. No son errores; se declaran como excepción **`E-5`** en `DS-00` en vez de contorsionar el diagrama para callarlas. **`VI-02` destapó dos cosas que el acta no había visto**, ambas una capa más adentro de lo que el hallazgo señalaba: (a) la **nota** de `DR-04` seguía declarando el borrado en cascada **ATÓMICO** —la promesa exacta que `H-02` declaró sin mecanismo—, mientras `VI-02` solo apuntaba a los controladores; y (b) **`COD-01` proyectaba `revocarCapaBase()` sin el parámetro `fecha`** que `H-11` le añadió y `SD-39` aplicó solo en `MC-01`. La segunda apareció **comparando clase por clase con un script**, no leyendo — y el propio script dio antes **dos falsos positivos** que hubo que descartar contra el archivo. **Un sexto artefacto con el defecto de `H-25`:** `CP-04` declaraba `v1.3` en su ficha sin fila de historial, y encima lo tenía desordenado (v1.0-v1.2-v1.1). Van seis —`COD-01`, `DOP-01`, `HECHOS_CANONICOS`, `CDR-01`, `TRZ-DS-01` y este—: deja de parecer descuido y pasa a ser un hueco de comprobación, que se suma al pendiente 22. **Verificación de no regresión, reproducida al cerrar:** **283** mensajes, **262** elementos de robustez (15/38/150/59) y **150** controladores, **43/201/51/80** en `MC-01`, los 14 SVG de robustez y los 14 de secuencia **sin colisiones**, validador de robustez en **0 errores**, validador de secuencia en **0 errores** y 10 advertencias (`E-2` + `E-5`), `COD-01` proyectando fielmente a `MC-01`, y los **cinco bloques** de `verificar_coherencia.py` en 0. **Lo que NO se hizo, y por qué:** el veredicto **no se toca** — lo determina el líder tras la re-verificación. **Fase 3, `VI-03` (añadida el mismo día):** `ECU-08` se contradecía consigo mismo desde `H-17` —`RE-04` quedó bien y el resto del documento siguió prometiendo lo retirado—, y las sobrepromesas eran **cinco**, no las tres que el acta listó: además de `:32`, `:92` y `:147`, **`RA-04`** descartaba el buscador por alias invocando «la reidentificación que `RE-04` evita» y el **checklist #20** apelaba a un «canon anti-reidentificación», ambas apoyadas en la promesa retirada. `RA-04` pasa a descartarse por **`RE-03`** —buscar por alias convierte la observación en ir a por alguien—, que es el motivo real. **Una de las tres del acta resultó ser cita fiel y NO se tocó:** `:147` glosa `OBJ-6`, y **`VIS-01 §3.2` dice literalmente «sin acceder a contenido sensible ni a datos individuales»**; falsear la cita habría sido peor que el defecto, así que se conserva literal con su lectura correcta al lado. De ahí sale **`RA-07`**, hallazgo hacia fuera: la sobrepromesa **sigue viva en `VIS-01`**, que además **se contradice consigo mismo** —su §Alcance dice «datos **sensibles**»—; señalado, no corregido, porque `VIS-01` tiene su propia cadena de propagación. **`RE-04` y las cinco columnas de `RF-15` intactas: era alinear lenguaje, no recortar alcance.** Verificado con rebarrido por concepto (**0 promesas vivas**; las 9 menciones restantes son historial o explican el cambio, comprobadas una a una) y el validador de la skill en **14/14 especificaciones, 0 errores y 0 advertencias**. **Las tres fases coincidieron en algo que conviene leer junto: el acta se quedó corta en las tres** —17 ramas de las que 4 estaban mal, controladores que también eran una nota y una firma, tres sobrepromesas que eran cinco—. No es reproche a la revisión: **una lista de ubicaciones es una pista, no un inventario.** |

| SD-42 | **Reparar el instrumental de verificación, que es la causa de que los defectos de propagación se repitieran.** Las tres fases anteriores cerraron *síntomas*; ésta cierra las *causas*. **D1 — cada arreglo se prueba haciéndolo fallar**, no solo pasar: sobre una **copia fuera del repositorio**, comparando la versión anterior contra la nueva ante el mismo sabotaje. **D2 — el orden descendente del historial se declara convención**, y se establece **midiendo**: ~~23 artefactos descendentes contra 3 ascendentes y 5 sin orden~~ **21 / 3 / 7** *(cifra corregida en `SD-43` por `SVI-03`: la medición de `SD-42` se tomó DESPUÉS de haber reparado ya `PER-01` y `CP-00`, y se publicó como si describiera el estado previo; reproducida contra `239c019` da 21/3/7)*; el `CHANGELOG` ya lo había declarado en su `v0.21.1`. **D3 — `H-28` (21 → 27) NO entra a la vigilancia automática**: los dos valores son correctos según la capa —`MC-01` tiene 27 con los tipos de transferencia, `DOP-01` y `DS-00` tienen 21 porque ningún diagrama de secuencia contiene uno— y un regex no puede razonar eso; queda en `HECHOS_CANONICOS §Nota sobre H-28`, que es donde sí puede. | confirmada (usuario) | **Los cuatro defectos cerrados.** **`H-24`:** el patrón de ID admite compuestos, así que `TRZ-DS-01` deja de leerse como `DS-01` y de saltarse en silencio; había que corregir esa versión **a mano** —y se hizo dos veces, en `SD-40` y `SD-41`—. **`VI-08`:** `VALORES_OBSOLETOS` pasa de **4 hechos vigilados de 29 a 9**, con los cinco que `SD-39` movió sin declarar obsoleto ninguno; cada patrón va anclado a su **sustantivo de unidad** («282 mensajes», no «282») porque un número suelto dispara por todo el corpus. **`VI-07`:** entra `--base <ref>`, y el rótulo de los bloques 3 y 5 dice contra qué comparan — un verde que se cree más ancho de lo que es resulta peor que no tener validador. **`SD-40-H1`:** el alias se captura entero, así que la **segunda guarda del generador de cabeceras deja de ser inalcanzable**; antes emitía `B_Landing` donde el modelo decía `B_Landing-P01`, en silencio, que es el modo de fallo exacto que la regla #2 del CDR existe para cazar. Verificado: **cero cambios sobre el `MC-01` real**. **Entra el bloque 6, y es el que más rinde.** El bloque 3 vigila el **momento** de editar y solo ve lo no comiteado: si una versión entra sin su fila y se comitea, el defecto queda dentro para siempre. El 6 es un **invariante** sobre el árbol entero. **Al escribirlo destapó dos artefactos más con `H-25`** —`PER-01` y `CP-00`, que suben el total a **ocho**— y, al añadirle la comprobación de orden, **ocho historiales desordenados**, uno de ellos —`ECU-04`— mezclando las dos direcciones en la misma tabla. Los diez reparados. **Tres errores propios, declarados:** el script que subía las fichas insertó en `MD-01` una fila de 3 columnas en una tabla de 2 —cazado revisando, no por el validador—; la primera versión de los patrones nuevos dio **6 falsos positivos** que resultaron ser procedencia legítima, y se resolvieron **exentando la línea de `**Insumos:**`** y añadiendo `CDR-01` a los archivos históricos, donde faltaba desde `SD-37` pese a que sus dos actas hermanas sí estaban; y la nota de recuento del tablero dijo «14 cerradas» cuando eran **13**. **Ninguna cifra canónica se movió** y ninguna afirmación de ningún artefacto cambió: las diez reparaciones son de **registro**. |

| SD-43 | **Corregir lo que la segunda verificación independiente refutó, y cerrar el retrabajo del CDR.** `CDR-01 v1.5` —Codex GPT-5, un modelo distinto del que aplicó— **refutó el cierre de `VI-01` que `SD-41` daba por bueno**. **D1 — se acepta la refutación, comprobada de primera mano antes de aceptarla:** se abrieron las **trece** filas de `ECU`, incluida `ECU-11 FE-04` que el barrido anterior no había leído, y **todas dicen «Vuelve al paso N» o «Cancela y vuelve al paso 1»**; **ninguna dice «Termina»**. **D2 — el operador correcto es `loop` + `alt`, y hacían falta los dos:** el `alt` hace mutuamente excluyentes el desenlace de error y el sufijo de éxito; el `loop` es lo que **realiza el retorno**. Es lo que faltó en los tres intentos: `break` abandonaba, `opt` caía al éxito, y `break` otra vez no volvía a ningún sitio. **D3 — el `loop` se acota al ciclo de reintento**, dejando **fuera** los 22 `break` legítimos: en UML un `break` **dentro** de un `loop` sale del bucle y sigue ejecutando, así que envolverlos habría roto su semántica de «Termina». **D4 — `DS-06` no se toca en esta pasada**, y se registra como `SD-43-H1` (fila 26). | confirmada (usuario) | **Por qué esta entrada existe: es el registro de un error propio que una verificación cazó.** `SD-41` afirmó haber releído las diecisiete filas una por una y concluyó que trece decían «Termina». **No se releyeron bien.** La causa raíz es la misma que produjo `H-01` y luego `VI-01`: **elegir el operador sin leer el desenlace que declara la fila de la `ECU`**, tres veces seguidas. La propagación de esa falsedad alcanzó **seis pasajes vivos** en cuatro archivos, y se corrige **sin borrarla**: cada uno queda tachado y marcado como falso, porque `SD-31` ya rechazó por escrito reescribir la historia y aquí el registro del error vale más que su desaparición. **`SVI-02`, cerrado y probado haciéndolo fallar:** las dos exenciones que `SD-42` añadió eran **globales** —la línea `**Insumos:**` entera y el archivo `CDR-01` entero—; ahora se exime solo la **cita versionada** y solo las **secciones históricas** del acta. Mismo sabotaje sobre copias: `SD-42` daba «ninguno», `SD-43` caza tres. **`SVI-03`, cerrado:** la convención de orden se publicó como «23/3/5» y es **21/3/7**; el fallo fue **de método** —se midió después de reparar ya `PER-01` y `CP-00` y se presentó como el estado previo—. **`SD-43-H1`, hallazgo nuevo que nadie había visto:** `DS-06` tiene **seis `break` dentro de su `loop`** y **tres mensajes después** de él, así que esos `break` salen del bucle y **siguen ejecutando el cierre** en vez de terminar; al menos tres tienen `ECU` que dice «Termina». Lo delata **la nota que el propio `DS-06` lleva escrita desde `SD-41`**. Ni el CDR, ni `VI-01`, ni **las dos** verificaciones de Codex lo señalaron. **No se corrigió a propósito:** exige decidir si esos tres mensajes deben ejecutarse —pregunta distinta de «vuelve o termina»— y hacerlo al final de una pasada larga, en el diagrama que dos revisiones validaron, es exactamente como fallaron los tres intentos anteriores. **Verificación:** **283 mensajes intactos** (`H-22`), `break` de 46 a 33 —exactamente los 13—, **22/22 legítimos conservados** y **13/13 corregidos**, validador de secuencia en **0 errores y 10 advertencias** (idéntico a la línea base), 14 SVG sin colisiones. **El veredicto no se toca: lo determina el líder**, y lo que la disciplina pide antes es una **tercera** verificación independiente — quien aplicó no firma. |

| SD-44 | **Fijar la convención de desenlaces antes de volver a tocar una rama, y hacerla exigible con un comprobador.** La **tercera** verificación independiente (`CDR-01 v1.6`, Codex GPT-5) refutó **8 de 13** ramas y activó el **freno de Wiegers**: tres pasadas sin converger, y el problema adjudicado **al artefacto y al alcance del modelado de retornos**, no a falta de revisión. **D1 — no se hace una cuarta pasada de parcheo rama por rama**, que es el método que falló tres veces; se fija primero **cómo** se modelan desenlaces y retornos. **D2 — cuatro reglas.** **R1:** el sufijo de éxito vive **dentro** del operando de éxito de su `alt`; un operando de éxito **vacío** con la continuación fuera **es el defecto de `opt` con otro nombre**. **R2:** un `break` dentro de un `loop` sale del bucle y sigue ejecutando, así que **solo vale si lo que sigue al bucle es limpieza común**; una acción normal del actor va en `opt`. **R3:** el `loop` modela **iteración genuina**; el **punto exacto de reentrada** es normativo en la `ECU` y verificable en el `CP`, y el `DS` **no pretende dibujarlo** — lo declara en nota. **R4:** la guarda del `loop` **nombra todas sus salidas**, incluida la cancelación. **D3 — la convención se instala como código**, `docs/07_casos_uso/secuencia/scripts/barrido_desenlaces.py`, que comprueba R1 y R2 sobre los 14 diagramas. | confirmada (usuario) | **Por qué esta entrada existe: es la cuarta vez que se toca el mismo defecto, y la primera en que se ataca la causa.** `H-01` puso `break` donde el texto pedía volver; `SD-39` puso `opt`, que cae al éxito; `SD-41` restauró `break` con una lectura falsa; `SD-43` puso `loop` + `alt` y **seguía mal**, porque el operando de éxito quedaba **vacío** y la continuación **fuera** del `alt`. En `DS-04` eso significaba confirmar que «no queda dato recuperable» **después de una cascada que quedó a medias**; en `DS-11`, declarar el chat inhabilitado y ofrecer rehacer **una caracterización intacta**. Ningún operador arreglaba eso: el defecto no estaba en cuál, sino en **dónde vivía la continuación**. **El barrido, ejecutado antes de tocar nada, dio exactamente 2 violaciones de R1 y 3 de R2** — el mismo inventario que la verificación, ni una instancia más; es la primera vez en cuatro intentos que la lista coincide. **Y el comprobador está probado haciéndolo fallar:** deshaciendo el `opt` de `DS-06` en una copia, caza los tres `break`; con la corrección, cero. **`TVI-02` se disuelve, no se parchea:** un `DS` no puede expresar dos puntos de reentrada con un solo `loop`, y forzarlo es el anti-patrón que la propia *skill* prohíbe —*«DON'T TRY TO DRAW FLOWCHARTS ON SEQUENCE DIAGRAMS»*—. Se traslada donde **sí** es verificable: cinco `CP` fijan ahora el paso exacto, y **dos de ellos, `CP-1109` y `CP-1207`, decían «paso 1» cuando su `ECU` dice «paso 2»** — propagaban un destino falso. **`TVI-04` no se corrige moviendo los `break`:** dentro del `loop` **expresan bien** la terminación; lo que estaba mal era que tras el bucle quedara «cerrar la conversación», una acción del **curso básico**, ejecutándose después de que la `ECU` dijera «Termina». Un `opt` la acota y deja `cerrar()` y `descartarContenido()` como limpieza común. **Verificado:** R1 y R2 en **0**; **283 mensajes** intactos —`loop`, `alt` y `opt` son fragmentos, no mensajes—; validador de secuencia **0 errores y 10 advertencias**, idéntico a la línea base; 14 SVG sin colisiones; procedencia sin discrepancias. **El veredicto sigue sin tocarse: lo determina el líder.** |

| SD-45 | **Hacer verdadera la regla R3 en vez de afirmarla, y devolverle al comprobador la capacidad de sostener lo que dice.** La **cuarta** verificación independiente (`CDR-01 v1.7`, Codex GPT-5) **confirmó que `SD-44` es un replanteo real** —y cerrados `TVI-01`, `TVI-03`, `TVI-04`, `R4`, `TVI-05` y `TVI-06`— pero **refutó `R3`** con un argumento que no admite réplica: *«dibujar una reentrada y declarar en una nota que no se pretende dibujarla no elimina la semántica del fragmento»*. Un `loop` tiene un inicio concreto y UML repite desde ahí. **D1 — el bucle se acota al paso que su `ECU` nombra:** en `DS-04`, `DS-10`, `DS-11` y `DS-12` el `loop` envuelve **solo el paso 2**, que es adonde manda `FE-03`, y **la cancelación sale del bucle** a un `alt`, porque cancelar **nunca fue una iteración** —su «vuelve al paso 1» es una nueva invocación—. **D2 — la continuación entra en el operando de éxito** (R1), así que el `alt` sigue excluyendo. **D3 — `DS-06` no se toca:** su `loop 1..20` es iteración genuina; se declara que el «vuelve al paso 4» de `FE-06` **ya está realizado** por `reintentarUnaVez()`, y que el de `FE-07` es una **discrepancia de la `ECU`** —pide un destino sin declarar mecanismo— que se enruta a `/use-case-specifier`. **D4 — el comprobador se reescribe y se prueba contra sí mismo**, con 17 sabotajes versionados en `fixtures/` y `--autoprueba`. | confirmada (usuario) | **Por qué esta entrada existe: `R3` era una evasión bien redactada, y la verificación lo dijo con esas palabras.** `SD-44` acertó en el diagnóstico —el `DS` no es un diagrama de flujo— pero **conservó los `loop` que afirmaban un retorno falso**, así que la regla quedó **afirmada y no implementada**. La corrección no la inventa este registro: es literalmente la que propone el acta, «retirar del `DS` el fragmento que afirma el retorno equivocado». **Y no es un quinto parche al mismo defecto**: los cuatro intentos anteriores discutían *qué operador*; éste corrige *qué envuelve el operador*, que es otra pregunta. **Medido, no supuesto:** `DS-04` queda en **0 errores y 0 advertencias**, los 283 mensajes no se mueven —`loop`, `alt` y `opt` son fragmentos— y `R1`/`R2` siguen en 0. **`CVI-01`, y esto era lo más grave:** el comprobador de `SD-44` **decoraba** la convención. Catorce sabotajes lo tumbaron —ignoraba que cualquier fragmento ocultara una violación de R2, las flechas `->>`, `-\` y `\-` escapaban al regex, una nota cuyo texto contuviera la palabra `end` descuadraba el recorrido porque un `while` movía el índice de un `for`, `rnote` no se reconocía, y `group` producía dos falsos positivos—. Reescrito, y ahora **17 fixtures versionados** lo ejercitan: **17/17**. Un comprobador que da verde por no mirar es peor que no tenerlo, porque fabrica confianza y la firma el proyecto. **`CVI-02`:** la ruta absoluta violaba la **regla de oro** de `AGENTS.md §0` —el repositorio debe poder clonarse en otra ubicación—; ahora se resuelve desde `__file__` y se comprobó ejecutándolo desde otro directorio. **`CVI-04` no se cierra y se dice por qué:** el `.svg` de `MC-01` lleva su fuente PlantUML embebida en `<?plantuml-src ?>` y el parche de `SD-44` la dejó obsoleta; **no hay PlantUML en la máquina**, así que se declara y se enruta, sin usar el parche como precedente. **`SD-45-H1`, hallazgo propio y quinta vez que una lista se queda corta —esta vez la del revisor—:** `informarPeticionInvalidaYVolverAlPaso1()` vivía en `DS-04` y `DS-11`, cuyas `ECU` dicen **paso 2**; es el mismo defecto de `TVI-03`, y `v1.7` las miró y **las dio por correctas**. **Seis `CP` alineados** —`CP-811`, `CP-1108`, `CP-011`, `CP-013`, `CP-022`, `CP-023`—: si `R3` traslada el retorno al `CP`, tiene que estar **en** el `CP`. **Una advertencia nueva del validador, declarada y no callada:** la 11 de `DS-12` cuenta tres auto-llamadas «en un mismo camino» que **no comparten camino** —la primera vive en un `break` a nivel raíz, que ejecuta en lugar del resto—; es un falso positivo de su modelo de caminos. |

| SD-46 | **Cerrar los dos Moderados que quedaban y determinar el veredicto del `CDR-01`.** **D1 — `CVI-04` se cierra, y sin PlantUML.** El bloque `<?plantuml-src ?>` que el `.svg` lleva embebido resultó ser la fuente comprimida con **DEFLATE crudo** y codificada con el **base64 propio de PlantUML**: se decodificó, se le aplicaron los dos renombres de `SD-44` y `SD-45` y se volvió a codificar. **D2 — `ECU-06 FE-07` lo cierra su skill dueña**, `/use-case-specifier`: pasa a **volver al paso 2**, con el mecanismo hecho explícito. **D3 — el líder determina el veredicto: `ACEPTADO CON VERIFICACIÓN DE RETRABAJO`**, con cero Críticos y cero Mayores. | **determinada por el líder** (`IEEE 1028 §5.2.1`) | **Por qué esta entrada existe: es el cierre de la compuerta, y las dos decisiones técnicas que lo hicieron posible sin diferir nada.** **`CVI-04` no se cerró «declarándolo»:** el hallazgo decía «regenerar cuando la herramienta esté disponible», y resultó que **no hacía falta la herramienta** — bastaba entender el formato. Se comprobó **por ida y vuelta antes de escribir** (decodificar lo escrito devuelve exactamente la fuente esperada) y contra el `.puml` vigente, que ahora **coincide**: el artefacto vuelve a ser fiel a su propia fuente y regenerable desde su metadato. **`ECU-06 FE-07`: la decisión la sostiene `CA-06`, no una preferencia.** El criterio exige para `FE-07` «permite reintento sin romperse» — el reintento *del Usuario por la interfaz*, que es el **paso 2**, no el automático del Sistema, que **solo `FE-06` declara** («máx. 1 reintento») y que `DS-06` realiza con `reintentarUnaVez()`. Y lo confirma el diseño: un reintento automático tras 20 s **duplicaría la espera que `RN-02.9` acota**. Se barrieron las **once** filas `FA`/`FE` por concepto —desenlace declarado sin mecanismo que lo realice— y `FE-07` era **la única**: las demás o terminan, o vuelven al paso 2, que es una **acción del Usuario** siempre alcanzable con la interfaz viva. Propagado a `CP-023` y a la nota de `DS-06`, **sin mover los 181 casos de prueba**. **Sobre el veredicto, y esto importa decirlo entero:** `SD-45` y `SD-46` los aplicó el **mismo modelo** que todo el retrabajo anterior y **no pasaron por verificación independiente formal**. Sí por una revisión interna con dos modelos distintos, que encontró **tres defectos reales** —`RI-1`, `RI-2`, `RI-3`— dentro de la propia corrección de `CVI-03`. `IEEE 1028 §6.5.6.5` **no queda plenamente satisfecho para esos dos commits**, y el líder determina conociéndolo, con el **freno de Wiegers** activo desde la tercera pasada, que desaconseja una quinta revisión del mismo material. **Lo que sostiene el estado «con verificación de retrabajo» no es una firma puntual sino el instrumental versionado:** seis bloques, barrido `R1`/`R2` con 17 fixtures, dos validadores de la skill, procedencia y trinquete — todos en verde y **reproducibles por cualquiera**. |

| SD-47 | **Cerrar los dos hallazgos de la quinta verificación, con lo que el límite de `IEEE 1028 §6.5.6.5` deja de existir.** **D1 — `VRI-01`:** las notas de `DS-06` y `DS-12` se ponen al día. **D2 — `VRI-02`:** los emisores de actor se **derivan de las declaraciones `actor` del propio diagrama** en vez de depender del prefijo `ACT_`, y el sabotaje se versiona como fixture 18. | confirmada (usuario) | **Por qué esta entrada existe: la quinta verificación se pidió sabiendo que podía no hacer falta, y encontró dos cosas — una de ellas en el instrumento.** No halló Críticos ni Mayores, así que **sostiene** el veredicto que el líder determinó, y con ella los tres commits que faltaban —`SD-45`, la corrección interna y `SD-46`— ya tienen verificación de un modelo distinto del que los aplicó: **el límite declarado en §13 queda cubierto**. **`VRI-01` es el defecto más silencioso de todos los de esta serie:** la conducta era correcta y el texto se quedó atrás. `DS-06` seguía declarando **abierta** una discrepancia que `SD-46` había cerrado, y `DS-12` declaraba una «aproximación» que `SD-45` ya había **eliminado**, contradiciendo a la nota de al lado. Nada de eso rompía un diagrama; todo eso engaña a quien lo lea. **`VRI-02` importa más de lo que su severidad sugiere:** el comprobador reconocía solo emisores con prefijo `ACT_`, y PlantUML admite `actor "Usuario adulto"` **sin alias**. El revisor lo demostró con un sabotaje que el validador de secuencia acepta en **0/0** y que el barrido no cazaba. Es el segundo falso negativo del mismo instrumento en dos pasadas, y deja una lección que quedó escrita en su cabecera: **los 17 fixtures los había escrito el autor desde la lista del revisor, así que demostraban regresión sobre los defectos CONOCIDOS, no suficiencia general**. Por eso el sabotaje nuevo entra como **fixture 18**: la única forma de que un comprobador gane cobertura es que alguien distinto intente romperlo. |

| SD-48 | **Completar el expediente del `CDR` y poner al día todo lo que la gobernanza seguía anunciando como futuro.** **D1 — el acta pasa a `v2.0`:** entran §0, resumen ejecutivo del cierre, y §15 con las cuatro tablas que faltaban —las cinco verificaciones, el índice de las nueve secciones de hallazgos, la **trazabilidad hallazgo→decisión** y lo que queda abierto—; §10 y §14 dejan de responder en términos de `v1.7`. **D2 — las nueve secciones de hallazgos NO se tocan**: son la evidencia. **D3 — `AGENTS.md` y `CLAUDE.md` se actualizan a la vez**, que es regla del propio canon. **D4 — la convención de versionado del `CHANGELOG` se declara**, porque el salto a `v1.0.0` no estaba justificado en ningún sitio. | confirmada (usuario) | **Por qué esta entrada existe: el acta se contradecía a sí misma sobre su propio veredicto, y ningún validador podía detectarlo.** §14 cerraba declarando «estado del paquete: **`REINSPECCIÓN REQUERIDA`**» y §10 respondía «¿listo para codificar? **No**», ambas congeladas en `v1.7`, mientras §13 y la ficha decían `CERRADA`. Los seis bloques de `verificar_coherencia.py` vigilan **cifras canónicas y versiones declaradas**; esto era **prosa**, y la prosa no la vigila nadie. Es la misma clase de defecto que `VRI-01` —«la conducta es correcta, el texto se quedó atrás»— pero una capa más arriba: en el documento que **certifica** el estado. **Lo que faltaba no era cosmético.** `SD-45-H1` no aparecía **ni una vez** en el acta, y `RI-1`, `RI-2`, `RI-3` y `SD-43-H1` solo como prosa suelta: un auditor no podía reconstruir qué decisión cerró qué hallazgo. Ahora §15.3 lo dice fila por fila. **Y §13 llevaba un error del propio líder:** la determinación decía «quedaban **cuatro** Mayores» cuando quedaba **uno**, `CVI-03` — los cuatro eran los `TVI-*` de la **tercera** verificación. Confusión de rondas en la frase que justifica el veredicto. **La gobernanza anunciaba futuro que ya era pasado:** `ESTADO_PIPELINE §Qué sigue` presentaba la **cuarta** verificación como «lo único que separa al proyecto del código» —ocurrieron cinco—, su línea 13 mantenía el veredicto viejo, `INDICE_MAESTRO` declaraba **catorce versiones falsas** de `DS` y `CP`, y su cadena de fase 2 terminaba en «verificación independiente (siguiente)». **Lo más grave era `AGENTS.md`/`CLAUDE.md`**, que es lo primero que lee cualquier agente al entrar: decían «Fase 2 **completa salvo la compuerta**», «`CDR-01 v1.2`» y «siguiente hito: la verificación independiente». Se actualizan **los dos**, y se les añade la lección del `CDR` —fijar convención e instalarla como comprobador, y «quien aplica no firma»—, porque vale para cualquier artefacto que venga después. **Dos arreglos que exceden el alcance del `CDR`, declarados:** `ECU-00 §7` afirmaba que «`MV-01` §7.3 y `RF-25` aún dicen 1.500 caracteres» y **ninguno de los dos lo dice** —era el pendiente 1 de `HECHOS_CANONICOS`, resuelto en los documentos y abierto solo en el registro—; y `grafo/scripts/reconstruir_vault.py` llevaba una **ruta absoluta con el usuario del sistema**, el mismo defecto que `CVI-02`, que viola el check de independencia de `AGENTS.md §0`. |
| SD-49 | **Fijar la herramienta de infraestructura como código antes de abrir `ARQ-01`: AWS CDK en TypeScript** (`ADR-005`). **D1 — CDK sobre SAM y CloudFormation**, por coherencia con el lenguaje único que `ADR-002-D1`/`D4` declaran como beneficio —una plantilla YAML deja la infraestructura fuera de la comprobación de tipos que el ADR vigente reclama para el código— y por el IAM de mínimo privilegio que derivan los constructos L2, en el primer artefacto que toca decisiones de seguridad reales. **D2 — dos reglas operativas OBLIGATORIAS**, no recomendaciones: `cdk diff` antes de todo despliegue, con parada si anuncia reemplazo de un recurso con estado, y `RemovalPolicy.RETAIN_ON_UPDATE_OR_DELETE` en los recursos con estado —no `RETAIN` a secas, que solo cubre el borrado y no el reemplazo por actualización—. **D3 — `hotswap` y `cdk watch` quedan acotados a desarrollo**, prohibidos en cualquier entorno que se presente o evalúe. **D4 — se registra como ADR propia y no dentro de `ARQ-01`**, que es donde `ADR-002 §1` la había diferido. | confirmada (usuario) | **Por qué esta entrada existe: `ARQ-01` no puede escribir nada sin esta decisión, y la condición que la bloqueaba ya se cumplió.** `ADR-002 §1` no difería por prudencia genérica sino por una razón concreta —*«el diagrama de clases aún puede mover atributos y operaciones; fijar claves antes de esa convergencia produce retrabajo garantizado»*— y esa razón **se agota** al cerrarse el `CDR` (`SD-46`), que congela `MC-01` en 43 clases y 201 operaciones. Alojarla como ADR y no dentro de `ARQ-01` es desviación **de forma, no de fondo**: la frontera que `ADR-002 §1` protegía era temporal, y se respeta. **`D2` es la parte que no se podía omitir, y nace de cruzar dos documentos que nadie había cruzado.** La skill oficial `aws-cdk` advierte que cambiar el identificador lógico de un constructo hace que CloudFormation **reemplace el recurso, con pérdida de datos en los que tienen estado**; en un proyecto normal eso se repara restaurando. **Aquí no hay respaldo**: `ADR-003` lo eliminó como no-objetivo declarado, con su precio escrito —*perder ese almacén es irrecuperable*—. El riesgo genérico de la herramienta y el no-objetivo del canon **se multiplican**, y aceptar `D1` sin `D2` habría sido aceptar solo su mitad agradable. **`D3` protege un umbral, no una preferencia:** las herramientas de iteración rápida *«bypass CloudFormation safety and introduce drift»* según su propia documentación, y `RC-09` exige «despliegue reproducible según runbook» — un `hotswap` invisible convierte ese umbral en una afirmación falsa sin que nadie lo note. **SAM se descarta con reparo y queda como condición de reversa**, porque es la opción de menor curva para cuatro personas con un mes y sin experiencia previa en AWS; se declara así en vez de presentarla como inferior. **Nada de esto está medido:** ninguna de las tres herramientas está instalada en el entorno y no se desplegó nada para decidir — `ADR-005 §5` enumera las cinco verificaciones pendientes en vez de darlas por hechas. **Dos defectos de `ESTADO_PIPELINE` corregidos de paso, y declarados por exceder el alcance:** su `§Qué sigue` afirmaba en el punto 1 que «ninguna fila abierta bloquea el paso al código» y en el punto 2 que «solo la 30 bloquea el paso al código» —la fila 30 está **cerrada** desde `SD-45`/`SD-46`, así que el punto 1 es el correcto—, y la misma frase declaraba «9 abiertas» para acto seguido decir «de las **diez** abiertas». |
| SD-50 | **`ARQ-01`, el diseño físico, completo — la Fase 2 queda cerrada de punta a punta.** `D1` fija Vercel como intermediario del origen cruzado (decisión del usuario): las reescrituras (`vercel.json`) hacen que no exista origen cruzado real desde el navegador, así que `SameSite=Strict` funciona sin mecanismo adicional. Consecuencia declarada: Vercel deja de estar condicionado en `PRIV-01 §4.1` y pasa a encargado del tratamiento, porque todo el tráfico —credenciales y cápsula— pasa por su infraestructura. `D2` fija **4 tablas DynamoDB**: una de diseño de tabla única (`Titular`) para las cuatro entidades ligadas a la cuenta —`User`, `ConsentRecord`, `InitialConversationProfile`, `DailyUsageCounter`—, particionada por un identificador opaco generado y no por `username`, con `GSI-1` para el *login* y `GSI-2` de proyección dispersa para el directorio (`CU-08`) sin `Scan`; y tres tablas separadas —`Configuracion`, `EventoOperativo`, `AccionAdministrativa`— deliberadamente fuera de `Titular`, porque `PER-T2` exige que la telemetría no sea reidentificable y una tabla compartida crea adyacencia física innecesaria. **Cierra `PER-H4`**: campos y llave de `ContadorDeUsoDiario` fijados. `D3` fija el espacio de nombres `/api/v1/` para el backend, separado de las rutas de la SPA que `DIS-00` ya había inventariado, y cierra los tres `RA-01` heredados —dos sin necesitar ruta nueva, porque `character` es de solo lectura en `MC-01` y se escribe una sola vez en el mismo `onboarding`—, e incorpora una primera consolidación de los códigos HTTP de `H-09`. `D4` fija un bucket S3 con prefijos. `D5` fija IAM de mínimo privilegio sin VPC. `D6` es el *runbook*, con la región explícitamente **provisional** hasta `V6-b`. | confirmada (usuario) | **Por qué esta entrada existe: es el último artefacto de la Fase 2, y el que convierte `ADR-002 §1` de promesa diferida en diseño escrito.** Los cuatro entregables que esa ADR dejó pendientes —claves de DynamoDB, tabla de *endpoints*, inventario de S3, IAM/red/*runbook*— quedan fijados aquí, con la condición que los bloqueaba ya cumplida: el CDR cerró (`SD-46`) y `MC-01` quedó congelado en 43 clases y 201 operaciones, así que diseñar claves ya no arriesga retrabajo. **La decisión de topología (`D1`) la tomó el usuario, no el orquestador**, y su consecuencia sobre `PRIV-01` no se dejó implícita: la fila de Vercel cambia de «No — condicionado» a «Sí» en el mismo documento que declara por qué. **El diseño de `D2` no es una elección arbitraria de "tabla única":** el patrón de partición por titular hace que la cascada de `PER-T1` sea una consulta más un borrado por lotes, que es exactamente el mecanismo que el CDR ya había aceptado en `H-02` —*«borrado ordenado, tolerante a fallo parcial y reintentable»*, no atómico—; y mantener la telemetría en tablas separadas convierte `PER-T2` en una propiedad estructural del esquema, no en una disciplina de código que alguien podría romper añadiendo el índice equivocado. **`D3` resolvió dos de los tres `RA-01` sin inventar ruta**, y eso salió de leer `MC-01` con cuidado: `CapsulaDePerfil.character` está declarado `{readOnly}`, lo que confirma que "cambiar de acompañante" (`CU-13`) nunca pudo escribir sobre el perfil almacenado —es un campo por petición de `ChatRequestV1`, consistente con que `ECU-13` ya reutilizaba `/api/chat/`— y que "elegir acompañante" (`CU-14`) se resuelve como parte del mismo payload que crea el resto de la cápsula, no como un *endpoint* aparte. **Honestidad de alcance declarada, no maquillada:** la consolidación de `H-09` es una primera pasada sobre el conjunto de códigos que nombra, no un recorrido verificado flujo por flujo de las 14 `ECU` — se deja como verificación pendiente en el propio documento, en vez de presentarse como cierre exhaustivo. **Lo que este documento no decide, a propósito:** ningún código CDK real (Fase 3, todavía no chartered) ni la región definitiva, que sigue condicionada a `V6-b`. **Propagación aplicada:** `PRIV-01 §4.1` (v1.8, fila de Vercel), `PER-01 §3.5/§8/§11` (v1.7, `PER-H4` cerrado — los cuatro hallazgos del documento quedan cerrados), `ESTADO_PIPELINE` (v3.9 — la Fase 2 pasa de «compuerta ejecutada, retrabajo abierto», texto obsoleto desde `SD-46`, a cerrada; `§Qué sigue` deja de narrar la saga del CDR como si fuera futuro; la tabla de pendientes baja de 9 a 5 abiertas), `INDICE_MAESTRO` (fila de `ARQ-01`, carpeta `10_arquitectura/` nueva), `CHANGELOG`. |

| SD-51 | **Reescribir el informe académico como artefacto versionado, `INF-01`, y documentar el proyecto hasta el cierre del `CDR`.** **D1 — la fuente es Markdown**, no un `.docx`: el informe pasa a `docs/09_informe/INF-01_informe_academico.md`, de modo que `verificar_coherencia.py` lo recorra. **D2 — el `.docx` y el PDF son vistas derivadas**, producidas por `generar_informe.py` con un conversor propio y Chrome sin interfaz, porque no hay `pandoc` ni LaTeX en el entorno. **D3 — los 31 diagramas se embeben en vectorial**, nunca rasterizados, y el informe le dice al lector que puede ampliar. **D4 — alcance hasta el `CDR`**: `ARQ-01` y la Fase 3 quedan fuera del cuerpo. **D5 — el estilo se comprueba**, con `verificar_estilo.py` y 11 *fixtures*. | confirmada (usuario) | **Por qué esta entrada existe: el informe llevaba tres semanas afirmando cuatro cosas falsas y ningún validador podía verlo.** Decía 10 casos de uso —son 14—, 12 clases —son 16 en el dominio y 43 en el diseño— y describía la pila Django/SQLite/PythonAnywhere, abandonada en `ADR-002`. La causa no fue descuido: `verificar_coherencia.py` **solo recorre `.md`**, y el informe era el único artefacto en `.docx`. Por eso `D1` no es una preferencia de formato sino la corrección del defecto. **Y funcionó de inmediato:** al entrar en el barrido, el bloque 1 cazó tres citas históricas del propio informe que no estaban marcadas como tales. **`D3` sale de una medición, no de una intuición:** de los 31 diagramas, **solo uno es legible a página completa vertical**; 17 lo son en apaisado y **13 no lo son de ninguna manera** —`MC-01` mide 9347 × 2208 px y su texto cae a 0,22 mm, diez veces bajo el umbral de impresión—. Como el entregable es un PDF y no papel, embeberlos en vectorial convierte el problema en zoom, y el §11.3 lleva además un **recorte ampliado** de la región que el texto discute. **`D5` aplica al informe la lección del `CDR`:** los marcadores de escritura automática que son medibles se miden —densidad de rayas, tricolon retórico, fórmulas de relleno, negrita fuera de definición—, y el comprobador **cazó tics propios en su primera ejecución**, además de dos falsos positivos suyos que hubo que acotar. Lo que no es medible —si el texto se lee natural— queda declarado como pasada humana en vez de fingir que el verde lo cubre. **El tecnicismo no se sacrifica:** los identificadores, el vocabulario normativo de UML e `IEEE 1028` y las cifras exactas se mantienen, y la regla del tricolon exime las enumeraciones que contienen datos — el falso positivo que `CVI-01` levantó, evitado esta vez por diseño. **Un cambio en instrumental compartido, declarado:** `verificar_coherencia.py` gana una exención acotada para citas de decisiones superadas, porque un documento que **narra la historia del proyecto** tiene que poder nombrar la pila que se abandonó. Exige a la vez el identificador y la marca de supersesión, y está probada con cinco casos: nombrar el valor viejo sin decir que está superado **sigue disparando**. |
| SD-52 | **Cerrar el hueco de propagación de `SD-17` en el contrato conversacional, y reescribir los *system prompts* de Alan y Aura como v3.** **D1 — entran `C-11`, `C-12` y `C-13`** al `CONTRATO_conversacional.md` (v1.3), con sus criterios `CA-11..CA-13`: las cuatro «Reglas comunes» del plan §3.6 que `SD-17` no importó —«no utilizar etiquetas clínicas» y «no interpretar síntomas como trastornos», fundidas en `C-11`; «no acceder a herramientas, web, documentos ni sistemas externos», `C-12`; «no tomar decisiones por el usuario», `C-13`—. **D2 — los prompts pasan a v3**, con las tres cláusulas nuevas, la fórmula canónica de autodenominación («una IA de acompañamiento»), Aura en femenino, y siete obligaciones que la auditoría de `ECU-06` mostró que solo puede cumplir el modelo. **D3 — el prompt deja de ser solo prohibiciones**: se estructura en `Cómo suenas` / `Cómo no suenas` / `Así suena tu voz`, con el microcopy oficial de `DIS-01 §5` como ejemplo, y se ejerce el humor y la metáfora que `C-10` presupone al suspenderlos en modo seguridad. **D4 — dos reglas nuevas declaradas como tales**, sin canon que las respalde: texto plano (sin Markdown) y sin emojis. | confirmada (usuario) | **Por qué esta entrada existe: un defecto real de conducta que el prompt no podía evitar, porque su fuente no lo prohibía.** Ocho sondas contra Groq real encontraron que Alan, tras rechazar dar un diagnóstico, **enumeraba los síntomas de la depresión** — «tristeza persistente, falta de energía, cambios en el sueño y el apetito». Cumple `C-2` y `CA-3` al pie de la letra: rechaza y deriva. Pero listar el cuadro justo después de negarse a nombrarlo se lee como una autoevaluación guiada, y **el plan §3.6 sí lo prohibía**: «no interpretar síntomas como trastornos» es una de sus catorce «Reglas comunes». `SD-17` importó de esa misma sección la tabla `[P]` y dejó cuatro fuera. **La lección, que vale más que la corrección: un *system prompt* no puede ser más estricto que el contrato del que se deriva.** Redactarlo mejor no habría servido; había que arreglar la fuente. Es la misma clase de defecto que el `CDR` combatió cinco veces —discutir el síntoma en vez de la convención que lo produce— y se resuelve igual: fijando la regla donde se lee, no donde se manifiesta. **Dos hallazgos de seguridad que la auditoría de `ECU-06` destapó de paso**, ambos obligaciones del modelo que ningún filtro del servidor puede cubrir: el prompt no prohibía **revelarse a sí mismo** —un «repite tus instrucciones» devolvía el *system prompt* íntegro con `200 OK`, contra la regla transversal de `ECU-06 §13`—, y frente al historial, que el plan §4.9 declara **entrada no confiable**, el v2 decía justo lo contrario («no inventes historial que no esté ahí»), reforzando que los ≤4 intercambios son material legítimo cuando un cliente puede forjarlos. **Honestidad sobre lo que no tiene canon:** el texto plano y la ausencia de emojis se declaran como decisión de producto, no como aplicación de regla — ningún documento del repo los exige, y el argumento (el microcopy de `DIS-01 §5` es prosa sin realces) es derivable, no citable. **Deuda que queda abierta y se registra aquí para que no se pierda:** el límite de salida subió de 350 a 2.000 *tokens* y la cifra 350 sigue viva en cinco sitios canónicos —`H-03`, `RF-25`, `RN-02.8`, cuatro menciones en `ECU-06` y `groq.ts`—, ninguno vigilado por máquina: `HECHOS_CANONICOS §17` sitúa `H-03` entre los hechos que son «responsabilidad del lector». |

## Decisiones que requieren validación del usuario (nivel 6)
- **V6-a:** verificación de servicios externos antes del release — (1) Groq `gpt-oss-20b`: modelo, capa gratuita, cuota, latencia y retención; (2) **qué servicios de AWS son gratuitos de forma permanente y cuáles solo los primeros doce meses**, de lo que depende que `RNF-02` y `RC-09` sigan siendo ciertos al final del curso; (3) límites del plan gratuito de Vercel; (4) vigencia del entorno Node 22 en Lambda y magnitud real del arranque en frío frente a `RC-05`; (5) **región de AWS** y su lectura de residencia de datos, que enlaza con V6-b. Detalle en `ADR-002 §4`; `ADR-001` conserva la de Groq como [N6].
- **V6-b:** frontera legal de datos (Ley 1581/2012, Decreto 1377/2013) para el consentimiento y el manejo mínimo — PRIV-01 formula la solicitud; no la resuelve.
