{
  "name": "backend",
  "private": true,
  "version": "0.0.0",
  "description": "Handlers Lambda (src/) y mock server (mock/). La infraestructura CDK vive en backend/infra, workspace aparte.",
  "type": "module",
  "scripts": {
    "mock": "tsx mock/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "contrato-api": "*",
    "@aws-sdk/client-dynamodb": "^3.658.0",
    "@aws-sdk/lib-dynamodb": "^3.658.0",
    "@aws-sdk/client-secrets-manager": "^3.658.0",
    "@aws-sdk/client-s3": "^3.658.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0",
    "@types/aws-lambda": "^8.10.145"
  }
}
