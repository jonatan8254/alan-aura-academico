{
  "name": "backend",
  "private": true,
  "version": "0.0.0",
  "description": "Placeholder de workspace de backend real (CDK + handlers Lambda). Por ahora solo contiene el mock server para desbloquear al frontend.",
  "type": "module",
  "scripts": {
    "mock": "tsx mock/server.ts"
  },
  "dependencies": {
    "contrato-api": "*"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "@types/node": "^22.0.0"
  }
}
